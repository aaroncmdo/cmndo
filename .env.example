# ─── Supabase ───────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# ─── Stripe ─────────────────────────────────────────────
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# ─── Google SMTP (existing) ────────────────────────────
GMAIL_SMTP_HOST=smtp.gmail.com
GMAIL_SMTP_PORT=587
GMAIL_SMTP_USER=
GMAIL_SMTP_PASS=
GMAIL_SMTP_FROM=Claimondo <noreply@claimondo.de>

# ─── Sentry (KFZ-174) ──────────────────────────────────
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=

# ─── Analytics / Tracking (alle optional; nur gesetzt = aktiv) ──
# Google Analytics 4 Measurement-ID (Format G-XXXXXXXXXX). Aktiviert den
# gtag.js-Loader in src/app/layout.tsx — NUR auf claimondo.de/www, consent-gated.
NEXT_PUBLIC_GA4_ID=
# Google Ads Conversion-ID (Format AW-XXXXXXXXX). Teilt sich den gtag.js-Loader.
NEXT_PUBLIC_GADS_ID=
# Microsoft Clarity Project-ID (Session-Recording/Heatmaps). Consent-gated auf
# Public-Seiten (laedt erst nach "Alle akzeptieren").
NEXT_PUBLIC_CLARITY_ID=
# Google Tag Manager Container-ID fuer den GF-Embed (/embed/gutachter-finder),
# Format GTM-XXXXXXX. BEWUSST server-seitig + runtime-gelesen (KEIN NEXT_PUBLIC_: Var
# setzen + Restart statt Rebuild; siehe src/app/embed/gutachter-finder/page.tsx). Leer
# = kein GTM im Embed. Consent-Gating + Enhanced-Conversions konfiguriert GTM selbst.
GF_GTM_ID=

# ─── Resend (KFZ-174) ──────────────────────────────────
RESEND_API_KEY=
RESEND_FROM=Claimondo <noreply@claimondo.de>
# Optional: Reply-To Adresse (default: gleich wie RESEND_FROM)
# RESEND_REPLY_TO=support@claimondo.de

# ─── App ────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=https://app.claimondo.de
CRON_SECRET=
# BUG-105: HMAC-Secret fuer Realtime-Channel-Hashing (32 byte hex, z.B. openssl rand -hex 32)
SUPABASE_REALTIME_SECRET=

# ─── Google APIs ────────────────────────────────────────
NEXT_PUBLIC_GOOGLE_MAPS_KEY=
GOOGLE_MAPS_SERVER_KEY=
GOOGLE_VISION_API_KEY=
GOOGLE_CALENDAR_CLIENT_ID=
GOOGLE_CALENDAR_CLIENT_SECRET=

# ─── Mapbox (AAR-380 Block G Field-Modus) ──────────────
NEXT_PUBLIC_MAPBOX_TOKEN=

# ─── Feldmodus Phase 4 — 3D Tiles ──────────────────────
# OPTION A: Cesium Ion (BEVORZUGT — in EEA verfügbar)
# Free-Tier: 5GB / 100k Requests/Monat, Pro ab $50/Monat.
# Asset 2275207 = Cesium OSM Buildings (default, fotorealistic genug).
NEXT_PUBLIC_CESIUM_ION_TOKEN=
NEXT_PUBLIC_FELDMODUS_CESIUM_TILES=false
NEXT_PUBLIC_CESIUM_ION_ASSET_ID=2275207

# OPTION B: Google Photorealistic 3D Tiles (in EEA seit 2024 GESPERRT
# wegen DSA/DMA-Regulierung — nur außerhalb der EEA nutzbar)
NEXT_PUBLIC_GOOGLE_MAPS_3D_TILES_KEY=
NEXT_PUBLIC_FELDMODUS_GOOGLE_TILES=false

# ─── Feldmodus HERE Hazards (Verkehrshindernisse) ──────
# Speed-Cameras NICHT verfügbar via HERE Standard-API.
# Aber: Baustellen, Sperrungen, Pannen-Fahrzeuge, Unfälle.
NEXT_PUBLIC_HERE_API_KEY=

# ─── Feldmodus ElevenLabs TTS (schönere Voice) ─────────
# Free-Tier 10k Characters/Monat. Fallback auf Web Speech API.
# Server-Side-Key (sk_-Prefix), läuft über /api/elevenlabs/tts Proxy.
# https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=

# ─── Cardentity (Vorschaden-Recherche) ─────────────────
# Default-URL: https://api.cardentity.eu (EU-Region). Override hier
# wenn ihr eine andere Region nutzt.
CARDENTITY_API_URL=
CARDENTITY_CLIENT_ID=
CARDENTITY_CLIENT_SECRET=
# Optionaler Direct-Token-Override für Smoke-Tests bevor OAuth steht.
# Cardentity-Tokens sind nur 1h gültig — danach im Dashboard neuen Token
# generieren. Wenn gesetzt, springt getAccessToken() OAuth komplett aus.
CARDENTITY_ACCESS_TOKEN=

# ─── Twilio ─────────────────────────────────────────────
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_VERIFY_SERVICE_SID=
TWILIO_WHATSAPP_FROM=

# Twilio WhatsApp Content Templates (nach Approval in Twilio Console)
# Format: HXxxxxxxxxxxx — leer lassen bis Templates genehmigt sind (Legacy-Fallback aktiv)
TWILIO_TPL_FALL_EROEFFNET=
TWILIO_TPL_FLOWLINK_VERSAND=
TWILIO_TPL_SV_BEAUFTRAGT=
TWILIO_TPL_TERMIN_BESTAETIGT=
TWILIO_TPL_REMINDER_24H=
TWILIO_TPL_REMINDER_2H=
TWILIO_TPL_SV_TAGESROUTE=
TWILIO_TPL_GUTACHTEN_FERTIG=
TWILIO_TPL_KANZLEI_UEBERGABE=
TWILIO_TPL_AS_GESENDET=
TWILIO_TPL_REGULIERUNG_ANGEKUENDIGT=
TWILIO_TPL_ZAHLUNG_EINGEGANGEN=
TWILIO_TPL_FALL_ABGESCHLOSSEN=
TWILIO_TPL_ESKALATION_TAG14=
TWILIO_TPL_ESKALATION_TAG28=
TWILIO_TPL_ESKALATION_TAG42=
TWILIO_TPL_CHAT_FALLBACK_KUNDE=
TWILIO_TPL_CHAT_FALLBACK_KB=
TWILIO_TPL_KUERZUNG_EINGETRAGEN=
TWILIO_TPL_SV_LOSGEFAHREN=
TWILIO_TPL_SV_FAST_DA=
TWILIO_TPL_SV_ANGEKOMMEN=
TWILIO_TPL_TERMIN_STORNIERT=
TWILIO_TPL_SV_VERSPAETET=
TWILIO_TPL_DOKUMENTE_NACHREICHEN=
TWILIO_TPL_RECHNUNG_VERFUEGBAR=

# ─── Lexoffice ──────────────────────────────────────────
LEXOFFICE_API_KEY=

# ─── Google Ads ─────────────────────────────────────────
GOOGLE_ADS_DEVELOPER_TOKEN=
GOOGLE_ADS_CUSTOMER_ID=
GOOGLE_ADS_REFRESH_TOKEN=

# ─── Anthropic ──────────────────────────────────────────
ANTHROPIC_API_KEY=

# ─── Linear (AAR-518 Support-Bot) ──────────────────────
# Personal API Key aus Linear Settings > API, Team-ID aus der Team-URL
LINEAR_API_KEY=
LINEAR_TEAM_ID=

# ─── E2E Tests (KFZ-185) ───────────────────────────────
# Test-User Credentials (NICHT Production-User!)
TEST_ADMIN_EMAIL=test-admin@claimondo.de
TEST_ADMIN_PASSWORD=
TEST_SV_EMAIL=test-sv@claimondo.de
TEST_SV_PASSWORD=
# Optional: Vercel Preview URL statt localhost
PLAYWRIGHT_BASE_URL=http://localhost:3000

# ─── LinkedIn Auto-Posting (Company-Page) ──────────────
# Siehe docs/linkedin-auto-posting-setup.md fuer die einmalige Einrichtung.
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=
LINKEDIN_ORG_ID=            # numerische ID ODER urn:li:organization:<id>
LINKEDIN_REDIRECT_URI=https://app.claimondo.de/api/auth/linkedin/callback
MARKETING_FEED_URL=https://claimondo.de/feed.json

# ─── Web-Push (VAPID, AAR-499) ─────────────────────────
# Neues Schluesselpaar erzeugen: npx web-push generate-vapid-keys
# Beide werden zur Laufzeit SERVER-seitig gelesen (web-push.ts sendet, health-check
# config-required-env prueft). Solange kein Client-Subscribe-Flow (pushManager.subscribe)
# existiert, ist NEXT_PUBLIC_VAPID_PUBLIC_KEY ein Runtime-Lookup (nicht build-inlined) —
# ENV setzen + Neustart genuegt, kein Rebuild.
# ACHTUNG: Der Server-Send-Pfad ist damit bereit, ABER der Client-Subscribe-Flow fehlt
# noch -> ohne ihn entstehen keine push_subscriptions (Push liefert 0).
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_CONTACT_EMAIL=tech@claimondo.de

# --- Marketing Content-Studio (Slice 1) ---
PEXELS_API_KEY=                          # Stock-B-Roll (gratis: https://www.pexels.com/api/). Ohne Key -> nur Grafik-Fallback.
ELEVENLABS_VOICE_ID=HNYELfQMgCeL9N0RGyxo # Deutsche ElevenLabs-Stimme (public, kein Secret)
MARKETING_STUDIO_ENABLED=true            # Kill-Switch (false = Generierung global aus)
MARKETING_MAX_CLIPS_PER_WEEK=20          # Kosten-Cap (max. Clips/Woche)
MARKETING_RENDER_MIN_RAM_MB=650          # RAM-Gate: Render startet erst ab X MB frei (OOM-Schutz shared VPS); leer/0 = Default 650
MARKETING_RENDER_DETACHED=false          # true = Render als detached Prozess (ueberlebt Deploy-Restart, scripts/render-detached.mjs); false/leer = inline im Web-Prozess
# Piper-Fallback (OSS-TTS wenn ElevenLabs 402/fehlschlaegt) - nur VPS:
PIPER_BIN=piper                          # oder voller Pfad zum piper/python-Binary
PIPER_MODEL=                             # Pfad zur .onnx-Stimme (z.B. .../de_DE-thorsten-medium.onnx)

# ─── Slice 2c — Unfallmeldung an die Haftpflicht des Unfallgegners ───────────
# Diese Meldung geht per E-Mail an ECHTE Versicherer (versicherungen.schaden_email).
# Default (unset) = true = prod sendet.
#
# ⚠ Auf STAGING/DEV zwingend auf false setzen. SIDE_EFFECT_MODE schuetzt hier NICHT:
# es steht per Default auf 'live' und filtert nur INTERNE Empfaenger (@claimondo.de) —
# eine echte Versicherer-Adresse ist extern und wuerde real angeschrieben.
# false = Dry-Run: alles wird gerendert + in vs_korrespondenz protokolliert (status
# 'archiviert', Notiz "[DRY-RUN]"), nur der Versand selbst unterbleibt.
VS_MELDUNG_ENABLED=false

# ── Test-Konten (Smokes/E2E) ────────────────────────────────────────────────
# Die Passwoerter stehen seit dem Leak vom 31.08.2026 NICHT mehr im Repo
# (siehe docs/2026-08-31-secret-leak-postmortem.md). Kontospezifisch liegen sie
# als GitHub-Secrets: TEST_ADMIN_PASSWORD, TEST_DISPATCH_PASSWORD, TEST_SV_PASSWORD,
# TEST_KANZLEI_PASSWORD, TEST_KB_PASSWORD, TEST_MAKLER_PASSWORD, SV_PASS.
# TEST_PASSWORT ist der generische Fallback fuer aeltere Smoke-Scripts, die kein
# kontospezifisches env hatten. Leer lassen = die betroffenen Scripts scheitern
# beim Login (bewusst: lieber ein sichtbarer Fehlschlag als ein Klartext-Passwort).
TEST_PASSWORT=
