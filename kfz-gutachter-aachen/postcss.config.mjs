// Tailwind v4 PostCSS-Plugin (identisch zu autounfall-io / claimondo-v2).
// Tokens in app/globals.css @theme.
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}

export default config
