# Kfz-Gutachter Wuppertal — ENV (Stub). Auf VPS: /etc/kfz-gutachter-duesseldorf/.env.local
# (chmod 600, root:root) → Symlink ins Projekt-cwd. Siehe DEPLOY.md.

# --- Site ---
NEXT_PUBLIC_SITE_URL=https://kfz-unfallgutachter-duesseldorf.de

# --- Monika-Embed (Plan 2 / Phase 2) ---
# Leer = aus (MonikaEmbedSlot rendert null). "true" = aktiv.
NEXT_PUBLIC_EMBED_BASE=https://claimondo.de
NEXT_PUBLIC_MONIKA_EMBED_ENABLED=

# --- Tracking (befuellt erst NACH Implementation, mit Aarons IDs) ---
NEXT_PUBLIC_GTM_ID=
NEXT_PUBLIC_GADS_AW_ID=
NEXT_PUBLIC_GADS_CONV_CALL=
NEXT_PUBLIC_GADS_CONV_WA=
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=
NEXT_PUBLIC_CLARITY_ID=
