src/app/admin/dispatch/lead/[id]/actions.ts:121:  console.log('[confirmGutachterTermin] START', { leadId, svId, termin, fahrzeugPlz, fahrzeugAdresse })
src/app/admin/dispatch/lead/[id]/actions.ts:139:  console.log('[confirmGutachterTermin] Lead updated ✓')
src/app/admin/dispatch/lead/[id]/actions.ts:170:    console.log('[confirmGutachterTermin] Fall updated ✓', { fallId: fall.id, sv_id: svId, termin_status: 'reserviert' })
src/app/admin/dispatch/lead/[id]/actions.ts:178:    console.log('[confirmGutachterTermin] Kein Fall vorhanden (wird bei SA-Unterschrift erstellt)')
src/app/admin/dispatch/lead/[id]/actions.ts:201:  console.log('[confirmGutachterTermin] gutachter_termine erstellt ✓', { sv_id: svId, start: termin, end: endDate.toISOString(), status: 'reserviert' })
src/app/api/cron/abrechnung-einzug/route.ts:201:  console.log(`[KFZ-149 einzug] success=${success} failed=${failed} total_pruefung=${faellig?.length ?? 0}`)
src/app/api/cron/abrechnung-kanzlei-reminder/route.ts:172:        console.log(`[KFZ-188 reminder] ${cfg.typ} fuer ${abr.rechnungsnummer} gesendet`)
src/app/api/cron/abrechnung-kanzlei-reminder/route.ts:183:  console.log(`[KFZ-188 reminder] gesendet=${gesendet} uebersprungen=${uebersprungen} fehlgeschlagen=${fehlgeschlagen} total=${abrechnungen?.length ?? 0}`)
src/app/api/cron/abrechnung-reminder/route.ts:104:  console.log(`[KFZ-149 reminder] T-7=${counts.reminder_7d} T-3=${counts.reminder_3d} T-1=${counts.reminder_1d} skipped=${skipped} failed=${failed} total_pruefung=${faellig?.length ?? 0}`)
src/app/api/cron/abrechnungen-faellig-check/route.ts:61:  console.log(`[faellig-check] ${updated} Abrechnungen auf ueberfaellig gesetzt`)
src/app/api/cron/community-leaderboard-update/route.ts:117:  console.log(`[KFZ-152] Community-Leaderboard updated: communities=${communities.length} eintraege=${totalEintraege}`)
src/app/api/cron/monats-abrechnungen/route.ts:63:  console.log(`[monats-abrechnungen] monat=${monat} marketing=${marketingCount} kanzlei=${kanzleiCount} errors=${errors.length}`)
src/app/api/cron/pflichtdokumente-reminder/route.ts:158:  console.log(`[KFZ-172] pflichtdokumente-reminder: ${faelle.length} Faelle geprueft, ${reminders} Reminder, ${completed} vollstaendig`)
src/app/api/cron/send-reminders/route.ts:229:  console.log(`[send-reminders] sent=${sent} failed=${failed} cancelled=${cancelled} skipped=${skipped}`)
src/app/api/cron/vollmacht-reminder/route.ts:151:  console.log(`[KFZ-192] vollmacht-reminder: ${faelle.length} geprüft, ${reminders} Reminder, ${tasks} Tasks`)
src/app/api/gutachter-matching/route.ts:68:  console.log(`[matching] Wunschtermin: ${wDate.toISOString()} (UTC ${fmtTime(wDate)})`)
src/app/api/gutachter-matching/route.ts:148:      console.warn(`[KFZ-154] gutachter-matching plz=${plz} spezifikation='${spezifikation}' kein passender SV — Fallback auf ${candidates.length} ohne Spez-Match`)
src/app/api/gutachter-matching/route.ts:153:  console.log(`[matching] ${workingCandidates.length}/${candidates.length} Kandidaten im Gebiet (KFZ-154 spez_filter=${!!spezifikation})`)
src/app/api/gutachter-matching/route.ts:187:  console.log(`[matching] DB: kalTermine=${kalTermine?.length ?? 0}, fallTermine=${fallTermine?.length ?? 0}, dayRange=${dayStart.toISOString()} bis ${dayEnd.toISOString()}`)
src/app/api/gutachter-matching/route.ts:225:      console.log(`[matching] ✅ ${svName}: FREI (keine Termine)`)
src/app/api/gutachter-matching/route.ts:233:        console.log(`[matching] ❌ ${svName}: BLOCKED — Slot ${fmtTime(slotStart)}-${fmtTime(slotEnd)} überschneidet ${fmtTime(t.start)}-${fmtTime(t.end)}`)
src/app/api/gutachter-matching/route.ts:244:        console.log(`[matching] ❌ ${svName}: BLOCKED by travel — prev endet ${fmtTime(prev.end)}, +${fahrzeit}min=${fmtTime(ankunft)} > ${fmtTime(slotStart)}`)
src/app/api/gutachter-matching/route.ts:255:        console.log(`[matching] ❌ ${svName}: BLOCKED by next — slot endet ${fmtTime(slotEnd)}, next um ${fmtTime(next.start)}, deadline ${fmtTime(deadline)}`)
src/app/api/gutachter-matching/route.ts:260:    console.log(`[matching] ✅ ${svName}: VERFÜGBAR um ${fmtTime(slotStart)} (${termine.length} Termine heute)`)
src/app/api/gutachter-matching/route.ts:321:  console.log(`[matching] Ergebnis: ${slots.length} Slots, sv_gesucht=${svGesucht}, verfügbar=${slots.filter(s => s.wunschtermin_moeglich).length}`)
src/app/api/isochrone/route.ts:17:    console.warn('[isochrone] HERE_API_KEY nicht gesetzt, nutze Fallback')
src/app/api/isochrone/route.ts:27:      console.warn(`[isochrone] HERE API ${res.status}: ${res.statusText}`)
src/app/api/isochrone/route.ts:35:      console.warn('[isochrone] HERE API: Kein Polygon im Response')
src/app/api/isochrone/route.ts:44:      console.warn('[isochrone] HERE API: Zu wenige Punkte')
src/app/api/isochrone/route.ts:50:    console.warn('[isochrone] HERE API Fehler:', err)
src/app/api/sv-zuweisung/route.ts:73:        console.log(`[KFZ-152] Lead ${fallId} im exklusiven Gebiet von Community ${ex.community_name} (org=${ex.organisation_id})`)
src/app/api/sv-zuweisung/route.ts:167:      console.warn(`[KFZ-154] sv-zuweisung fall=${fallId} spezifikation='${fall.spezifikation}' kein passender SV — Fallback auf ${candidates.length} ohne Spez-Match`)
src/lib/abrechnung/kanzlei/erstelle-abrechnung.ts:313:  console.log(`[KFZ-188] erstelleKanzleiAbrechnung ${monat}/${jahr}: erstellt=${erstellt} uebersprungen=${uebersprungen} fehler=${fehler}`)
src/lib/abrechnung/kanzlei/generate-pdf.tsx:200:    console.warn(`[KFZ-188 generate-pdf] Fehlende env vars: ${missingEnvs.join(', ')} — Platzhalter werden verwendet`)
src/lib/abrechnung/kanzlei/generate-pdf.tsx:235:  console.log(`[KFZ-188 generate-pdf] PDF generiert und hochgeladen: ${storagePath}`)
src/lib/abrechnung/process-case-billing.ts:76:  console.log(`[KFZ-149] Case ${fallId}: Lead-Preis=${leadPreis} (${typ}), Guthaben-Abzug=${guthabenAbzug}, Nachzahlung=${nachzahlung}, Guthaben-Neu=${guthabenNeu}`)
src/lib/copilot/post-call.ts:11:  if (!apiKey) { console.log('[KFZ-143] ANTHROPIC_API_KEY nicht gesetzt, überspringe Post-Call Analyse'); return }
src/lib/copilot/post-call.ts:69:  console.log(`[KFZ-143] Post-Call Analyse für ${callId} abgeschlossen`)
src/lib/email/google/client.ts:33:    console.warn(`[email] from-Override ignoriert: "${opts.from}" → nutze env RESEND_FROM/GMAIL_SMTP_FROM`)
src/lib/email/google/flows.ts:38:  if (alreadySent) { console.log(`[KFZ-137] Welcome-Mail für Fall ${fallId} bereits gesendet, skip`); return }
src/lib/finance/abrechnung-pdf.tsx:227:    console.warn(`[abrechnung-pdf] Fehlende env vars: ${missingEnvs.join(', ')} — Platzhalter werden verwendet`)
src/lib/finance/abrechnung-pdf.tsx:273:  console.log(`[abrechnung-pdf] PDF generiert: ${storagePath}`)
src/lib/finance/abrechnungen-generator.ts:59:    console.warn('[abrechnungen] MARKETING_MAIK_EMAIL nicht gesetzt — Marketing-Abrechnung übersprungen')
src/lib/finance/abrechnungen-generator.ts:75:    console.log(`[abrechnungen] Marketing-Abrechnung für ${monat} existiert bereits: ${existing.id}`)
src/lib/finance/abrechnungen-generator.ts:88:    console.log(`[abrechnungen] Keine signierten SAs im Monat ${monat} — keine Marketing-Abrechnung`)
src/lib/finance/abrechnungen-generator.ts:150:  console.log(`[abrechnungen] Marketing-Abrechnung ${abrechnungsNr} generiert: ${positionen.length} Positionen, ${fmtCurrency(summeBrutto)} brutto`)
src/lib/finance/abrechnungen-generator.ts:172:    console.log(`[abrechnungen] Keine abgeschlossenen Kanzlei-Fälle im Monat ${monat}`)
src/lib/finance/abrechnungen-generator.ts:258:    console.log(`[abrechnungen] Kanzlei-Abrechnung ${abrechnungsNr} für ${kanzleiName}: ${positionen.length} Positionen, ${fmtCurrency(summeBrutto)} brutto`)
src/lib/reminders/generate.ts:50:    console.log(`[reminder:generate] Termin ${terminId} hat Status "${termin.status}", überspringe`)
src/lib/reminders/generate.ts:101:    console.warn(`[reminder:generate] SV-Reminder für Termin ${terminId} konnte nicht berechnet werden (fehlende Koordinaten)`)
src/lib/reminders/generate.ts:115:  console.log(`[reminder:generate] ${reminders.length} Reminder für Termin ${terminId} generiert`)
src/lib/reminders/generate.ts:135:  console.log(`[reminder:cancel] ${count ?? 0} Reminder für Termin ${terminId} storniert`)
src/lib/reminders/sv-reminder.ts:46:    console.warn(`[sv-reminder] Keine Besichtigungsort-Koordinaten für Fall ${termin.fall_id}`)
src/lib/reminders/sv-reminder.ts:88:        console.log(`[sv-reminder] Termin→Termin Routing: ${Math.ceil(fahrzeitSek / 60)}min`)
src/lib/reminders/sv-reminder.ts:112:    console.warn(`[sv-reminder] SV hat keine Standort-Koordinaten, Default 30min`)
src/lib/reminders/sv-reminder.ts:116:  console.log(`[sv-reminder] Büro→Termin Routing: ${Math.ceil(sek / 60)}min`)
src/lib/routing/fahrtzeit.ts:77:  console.warn(`[routing] OSRM fehlgeschlagen, Fallback Luftlinie: ${km.toFixed(1)}km → ${Math.ceil(fallbackSek / 60)}min`)
src/lib/whatsapp/send-template.ts:81:    console.warn(`[KFZ-181] Kein Legacy-Text fuer Template '${templateName}', skip`)
src/lib/whatsapp.ts:24:  console.log(`[whatsapp] Sende an ${whatsappTo} von ${whatsappFrom}`)
src/lib/whatsapp.ts:34:    console.log('[whatsapp] Gesendet:', result.sid)
src/lib/whatsapp.ts:197:  console.log(`[whatsapp:manual] ${telefon} → success=${result.success}${result.error ? ` error=${result.error}` : ''}`)
