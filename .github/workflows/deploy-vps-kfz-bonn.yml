name: Deploy → VPS (kfz-unfallgutachter-bonn.de :3005)

# Auto-Deploy Cluster-LP Bonn. Muster identisch zu deploy-vps-kfz-wuppertal.yml
# (code-only Build in CI, atomarer Switch, persistente Asset-Symlinks, Auto-Rollback).

on:
  push:
    branches: [main]
    paths:
      - 'kfz-gutachter-bonn/**'
      - '.github/workflows/deploy-vps-kfz-bonn.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: [self-hosted, claimondo]
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install (bonn)
        working-directory: kfz-gutachter-bonn
        run: npm install --no-audit --no-fund
      - name: Build (standalone, code-only)
        working-directory: kfz-gutachter-bonn
        run: npm run build
        env:
          NODE_OPTIONS: --max-old-space-size=6144
          # Ortstiefe aus stadt_lokalinhalte (P3): Anon-Key + RLS-Policy, kein
          # Geheimnis — aber wie im Marketing-Deploy ueber Secrets gefuehrt.
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
          NEXT_PUBLIC_SITE_URL: https://kfz-unfallgutachter-bonn.de
          NEXT_PUBLIC_EMBED_BASE: https://app.claimondo.de
          NEXT_PUBLIC_MONIKA_EMBED_ENABLED: 'true'
          NEXT_PUBLIC_GTM_ID: GTM-KD2L63T3
      - name: Artefakt packen (standalone-Inhalt)
        working-directory: kfz-gutachter-bonn
        run: tar -czf ../deploy-kfz-bonn.tar.gz -C .next/standalone .
      - name: Deploy → VPS (SCP)
        uses: appleboy/scp-action@v0.1.7
        with:
          host: 212.132.119.110
          username: root
          key: ${{ secrets.VPS_SSH_KEY }}
          source: deploy-kfz-bonn.tar.gz
          target: /tmp/
      - name: Atomic switch + Asset-Symlink + restart :3005 (+ Auto-Rollback)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 212.132.119.110
          username: root
          key: ${{ secrets.VPS_SSH_KEY }}
          command_timeout: 10m
          script: |
            set -euo pipefail
            APP=/var/www/kfz-unfallgutachter-bonn-app
            ASSETS=/var/www/kfz-assets/bonn
            if [ ! -d "$ASSETS" ]; then echo "FATAL: $ASSETS fehlt"; exit 1; fi
            rm -rf "${APP}-new"; mkdir -p "${APP}-new"
            tar -xzf /tmp/deploy-kfz-bonn.tar.gz -C "${APP}-new"
            cat > "${APP}-new/.env.local" <<ENV
            NEXT_PUBLIC_MONIKA_EMBED_ENABLED=true
            NEXT_PUBLIC_SITE_URL=https://kfz-unfallgutachter-bonn.de
            NEXT_PUBLIC_EMBED_BASE=https://app.claimondo.de
            NEXT_PUBLIC_GTM_ID=GTM-KD2L63T3
            NEXT_PUBLIC_SUPABASE_URL=${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
            NEXT_PUBLIC_SUPABASE_ANON_KEY=${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
            ENV
            rm -rf "${APP}-new/public/assets/img"
            mkdir -p "${APP}-new/public/assets"
            ln -sfn "$ASSETS" "${APP}-new/public/assets/img"
            rm -rf "${APP}-old" 2>/dev/null || true
            mv "$APP" "${APP}-old" 2>/dev/null || true
            mv "${APP}-new" "$APP"
            pm2 delete kfz-gutachter-bonn 2>/dev/null || true
            PORT=3005 HOSTNAME=127.0.0.1 pm2 start "${APP}/server.js" --name kfz-gutachter-bonn --max-memory-restart 600M --cwd "$APP"
            pm2 save
            sleep 5
            code=$(curl -s -L -o /dev/null -w "%{http_code}" --max-time 20 "http://localhost:3005/")
            echo "Health / -> HTTP $code"
            if [ "$code" != "200" ]; then
              echo "HEALTH FAILED ($code) — ROLLBACK."
              pm2 delete kfz-gutachter-bonn 2>/dev/null || true
              rm -rf "$APP"; mv "${APP}-old" "$APP"
              if [ -f "${APP}/server.js" ]; then PORT=3005 HOSTNAME=127.0.0.1 pm2 start "${APP}/server.js" --name kfz-gutachter-bonn --max-memory-restart 600M --cwd "$APP"; else PORT=3005 HOSTNAME=127.0.0.1 pm2 start "${APP}/.next/standalone/server.js" --name kfz-gutachter-bonn --max-memory-restart 600M --cwd "${APP}/.next/standalone"; fi
              pm2 save; echo "Rollback fertig."; exit 1
            fi
            rm -f /tmp/deploy-kfz-bonn.tar.gz
            echo "Bonn-Deploy $(date) OK (HTTP $code)"
