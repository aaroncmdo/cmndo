# AAR-912 Dispatcher-Karte v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Erweitert die Dispatcher-Karte um SVs-Layer, Termine-Layer, LayerChipBar und Mapbox-Cluster — bei voller Whitelabel- und Realtime-Kompatibilität.

**Architecture:** Zwei neue Server-Loader (`getActiveSVs`, `getTermineToday`) und ein kombinierter Snapshot-Loader (`getKarteSnapshot`). Client wird auf Mapbox-GeoJSON-Sources mit `cluster:true` umgestellt (v1-Marker-Rendering ersetzt). Drei Popup-Komponenten plus eine Floating-Chip-Bar für Layer-Visibility-Toggles.

**Tech Stack:** Next.js 15 App-Router, Supabase (Server + Realtime), Mapbox-GL-JS (`addSource` + `addLayer`-Pattern), TypeScript, Vitest, Tailwind/Claimondo-Tokens.

**Linear:** [AAR-912](https://linear.app/aaroncmndo/issue/AAR-912/dispatcher-karte-v2-svs-layer-termine-layer-layerchipbar-cluster)
**Spec:** `docs/superpowers/specs/2026-05-14-dispatcher-karte-v2-design.md`

---

## File Structure

**Create:**
- `src/lib/dispatch/karte/resolve-termin-geo.ts` — pure Position-Priority-Chain Function
- `src/lib/dispatch/karte/resolve-termin-geo.test.ts` — Vitest unit tests
- `src/lib/dispatch/karte/get-active-svs.ts` — SV-Loader
- `src/lib/dispatch/karte/get-termine-today.ts` — Termin-Loader (nutzt resolveTerminGeo)
- `src/lib/dispatch/karte/get-karte-snapshot.ts` — Combined Snapshot Loader
- `src/app/dispatch/karte/LayerChipBar.tsx` — UI Chips
- `src/app/dispatch/karte/SVPopup.tsx` — SV Popup
- `src/app/dispatch/karte/TerminPopup.tsx` — Termin Popup

**Modify:**
- `src/lib/dispatch/karte/types.ts` — neue Types `LayerKey`, `SVPin`, `TerminPin`, `KarteSnapshot`
- `src/app/dispatch/karte/page.tsx` — nutzt `getKarteSnapshot` statt `getTriageLeads`
- `src/app/dispatch/karte/actions.ts` — neue `refetchKarteSnapshot`-Action
- `src/app/dispatch/karte/useTriageRealtime.ts` — Subscriptions auf `gutachter_termine` + `sachverstaendige` ergänzen
- `src/app/dispatch/karte/DispatchKarteClient.tsx` — Marker→GeoJSON-Source-Rewrite, 3 Layer, Cluster, LayerChipBar, Popup-Wiring

---

## Task 1: Types erweitern

**Files:**
- Modify: `src/lib/dispatch/karte/types.ts`

- [ ] **Step 1: Types ergänzen**

Anhängen an die bestehende `types.ts`:

```typescript
export type LayerKey = 'leads' | 'svs' | 'termine'

export type SVPin = {
  id: string
  vorname: string | null
  nachname: string | null
  firma_name: string | null
  paket: string | null
  stadt: string | null
  spezifikationen_top3: string[]
  bewertungs_durchschnitt: number | null
  bewertungs_anzahl: number | null
  lat: number
  lng: number
}

export type TerminPin = {
  id: string
  start_zeit: string
  status: string
  kunde_name: string | null
  sv_initialen: string | null
  fall_nummer: string | null
  fall_id: string | null
  lead_id: string | null
  lat: number
  lng: number
  geoSource: 'gps_ankunft' | 'lead_besichtigung' | 'sv_standort' | 'plz_centroid'
}

export type KarteSnapshot = {
  leads: TriageLeadPin[]
  svs: SVPin[]
  termine: TerminPin[]
  unlocalized: UnlocalizedLead[]
}

export type RawTerminForKarte = {
  id: string
  start_zeit: string
  status: string | null
  fall_id: string | null
  lead_id: string | null
  sv_id: string | null
  gps_lat_ankunft: number | null
  gps_lng_ankunft: number | null
  lead_lat: number | null
  lead_lng: number | null
  lead_vorname: string | null
  lead_nachname: string | null
  sv_lat: number | null
  sv_lng: number | null
  sv_vorname: string | null
  sv_nachname: string | null
  fall_nummer: string | null
}
```

- [ ] **Step 2: Typecheck**

Run:
```
npm run typecheck
```
Expected: grün.

- [ ] **Step 3: Commit**

```
git add src/lib/dispatch/karte/types.ts
git commit -m "feat(AAR-912): Types für SVPin, TerminPin, KarteSnapshot, LayerKey"
```

---

## Task 2: `resolveTerminGeo` pure function (TDD)

**Files:**
- Create: `src/lib/dispatch/karte/resolve-termin-geo.ts`
- Create: `src/lib/dispatch/karte/resolve-termin-geo.test.ts`

- [ ] **Step 1: Failing test schreiben**

`src/lib/dispatch/karte/resolve-termin-geo.test.ts`:
```typescript
import { describe, expect, it } from 'vitest'
import { resolveTerminGeo } from './resolve-termin-geo'
import type { PlzGeoRow, RawTerminForKarte } from './types'

const base: RawTerminForKarte = {
  id: 't-1',
  start_zeit: '2026-05-14T10:00:00Z',
  status: 'bestaetigt',
  fall_id: null,
  lead_id: null,
  sv_id: null,
  gps_lat_ankunft: null,
  gps_lng_ankunft: null,
  lead_lat: null,
  lead_lng: null,
  lead_vorname: null,
  lead_nachname: null,
  sv_lat: null,
  sv_lng: null,
  sv_vorname: null,
  sv_nachname: null,
  fall_nummer: null,
}

const plzMap = new Map<string, PlzGeoRow>([
  ['10115', { plz: '10115', lat: 52.53, lng: 13.38, ort: 'Berlin' }],
])

describe('resolveTerminGeo', () => {
  it('priorisiert gps_ankunft als Quelle wenn vorhanden', () => {
    const r = resolveTerminGeo({ ...base, gps_lat_ankunft: 51.0, gps_lng_ankunft: 13.7, lead_lat: 50.0, lead_lng: 8.0 }, plzMap, null)
    expect(r.kind).toBe('pin')
    if (r.kind !== 'pin') return
    expect(r.pin.geoSource).toBe('gps_ankunft')
    expect(r.pin.lat).toBe(51.0)
  })

  it('fällt auf lead_lat/lng zurück', () => {
    const r = resolveTerminGeo({ ...base, lead_lat: 50.1, lead_lng: 8.7 }, plzMap, null)
    expect(r.kind).toBe('pin')
    if (r.kind !== 'pin') return
    expect(r.pin.geoSource).toBe('lead_besichtigung')
    expect(r.pin.lat).toBe(50.1)
  })

  it('fällt auf sv_lat/lng zurück wenn lead keine Geo hat', () => {
    const r = resolveTerminGeo({ ...base, sv_lat: 48.13, sv_lng: 11.57 }, plzMap, null)
    expect(r.kind).toBe('pin')
    if (r.kind !== 'pin') return
    expect(r.pin.geoSource).toBe('sv_standort')
  })

  it('nutzt PLZ-Centroid via leadPlz wenn alles andere fehlt', () => {
    const r = resolveTerminGeo(base, plzMap, '10115')
    expect(r.kind).toBe('pin')
    if (r.kind !== 'pin') return
    expect(r.pin.geoSource).toBe('plz_centroid')
    expect(r.pin.lat).toBe(52.53)
  })

  it('liefert unlocalized wenn keine Quelle greift', () => {
    const r = resolveTerminGeo(base, plzMap, null)
    expect(r.kind).toBe('unlocalized')
  })

  it('füllt sv_initialen aus sv_vorname+nachname', () => {
    const r = resolveTerminGeo(
      { ...base, gps_lat_ankunft: 51, gps_lng_ankunft: 13, sv_vorname: 'Max', sv_nachname: 'Mustermann' },
      plzMap,
      null,
    )
    expect(r.kind).toBe('pin')
    if (r.kind !== 'pin') return
    expect(r.pin.sv_initialen).toBe('MM')
  })

  it('füllt kunde_name aus lead_vorname+nachname', () => {
    const r = resolveTerminGeo(
      { ...base, gps_lat_ankunft: 51, gps_lng_ankunft: 13, lead_vorname: 'Anna', lead_nachname: 'Schmidt' },
      plzMap,
      null,
    )
    expect(r.kind).toBe('pin')
    if (r.kind !== 'pin') return
    expect(r.pin.kunde_name).toBe('Anna Schmidt')
  })
})
```

- [ ] **Step 2: Test fails verifizieren**

Run:
```
npm run test -- resolve-termin-geo
```
Expected: FAIL — `resolveTerminGeo` existiert noch nicht.

- [ ] **Step 3: Implementation**

`src/lib/dispatch/karte/resolve-termin-geo.ts`:
```typescript
import type { PlzGeoRow, RawTerminForKarte, TerminPin, UnlocalizedLead } from './types'

type ResolveResult =
  | { kind: 'pin'; pin: TerminPin }
  | { kind: 'unlocalized'; lead: UnlocalizedLead }

function initialen(vorname: string | null, nachname: string | null): string | null {
  const v = vorname?.trim()[0] ?? ''
  const n = nachname?.trim()[0] ?? ''
  const out = `${v}${n}`.toUpperCase()
  return out.length > 0 ? out : null
}

function fullName(vorname: string | null, nachname: string | null): string | null {
  const parts = [vorname, nachname].filter(Boolean)
  return parts.length > 0 ? parts.join(' ') : null
}

export function resolveTerminGeo(
  termin: RawTerminForKarte,
  plzMap: Map<string, PlzGeoRow>,
  leadPlz: string | null,
): ResolveResult {
  const baseFields = {
    id: termin.id,
    start_zeit: termin.start_zeit,
    status: termin.status ?? 'unbekannt',
    kunde_name: fullName(termin.lead_vorname, termin.lead_nachname),
    sv_initialen: initialen(termin.sv_vorname, termin.sv_nachname),
    fall_nummer: termin.fall_nummer,
    fall_id: termin.fall_id,
    lead_id: termin.lead_id,
  }

  if (typeof termin.gps_lat_ankunft === 'number' && typeof termin.gps_lng_ankunft === 'number') {
    return {
      kind: 'pin',
      pin: { ...baseFields, lat: termin.gps_lat_ankunft, lng: termin.gps_lng_ankunft, geoSource: 'gps_ankunft' },
    }
  }
  if (typeof termin.lead_lat === 'number' && typeof termin.lead_lng === 'number') {
    return {
      kind: 'pin',
      pin: { ...baseFields, lat: termin.lead_lat, lng: termin.lead_lng, geoSource: 'lead_besichtigung' },
    }
  }
  if (typeof termin.sv_lat === 'number' && typeof termin.sv_lng === 'number') {
    return {
      kind: 'pin',
      pin: { ...baseFields, lat: termin.sv_lat, lng: termin.sv_lng, geoSource: 'sv_standort' },
    }
  }
  if (leadPlz) {
    const hit = plzMap.get(leadPlz)
    if (hit) {
      return {
        kind: 'pin',
        pin: { ...baseFields, lat: hit.lat, lng: hit.lng, geoSource: 'plz_centroid' },
      }
    }
  }

  return {
    kind: 'unlocalized',
    lead: {
      id: termin.id,
      vorname: termin.lead_vorname,
      nachname: termin.lead_nachname,
      firma_name: null,
      schadentyp: null,
      plz: leadPlz,
      created_at: termin.start_zeit,
    },
  }
}
```

- [ ] **Step 4: Tests grün**

Run:
```
npm run test -- resolve-termin-geo
```
Expected: 7 Tests PASS.

- [ ] **Step 5: Commit**

```
git add src/lib/dispatch/karte/resolve-termin-geo.ts src/lib/dispatch/karte/resolve-termin-geo.test.ts
git commit -m "feat(AAR-912): resolveTerminGeo Priority-Chain (gps→lead→sv→plz)"
```

---

## Task 3: `getActiveSVs` Server-Loader

**Files:**
- Create: `src/lib/dispatch/karte/get-active-svs.ts`

- [ ] **Step 1: Loader implementieren**

```typescript
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/lib/supabase/database.types'
import type { SVPin } from './types'

export async function getActiveSVs(supabase: SupabaseClient<Database>): Promise<SVPin[]> {
  const { data, error } = await supabase
    .from('sachverstaendige')
    .select(
      `id, vorname, nachname, firma_name, paket, stadt,
       standort_lat, standort_lng,
       spezifikationen_top3,
       bewertungs_durchschnitt, bewertungs_anzahl,
       ist_aktiv, portal_zugang_freigeschaltet` as 'id, vorname, nachname, firma_name, paket, stadt, standort_lat, standort_lng, bewertungs_durchschnitt, bewertungs_anzahl, ist_aktiv, portal_zugang_freigeschaltet',
    )
    .eq('ist_aktiv', true)
    .eq('portal_zugang_freigeschaltet', true)
    .not('standort_lat', 'is', null)
    .not('standort_lng', 'is', null)

  if (error) {
    console.error('[karte] sachverstaendige query failed', error)
    return []
  }

  type Row = {
    id: string
    vorname: string | null
    nachname: string | null
    firma_name: string | null
    paket: string | null
    stadt: string | null
    standort_lat: number | null
    standort_lng: number | null
    spezifikationen_top3?: string[] | null
    bewertungs_durchschnitt: number | null
    bewertungs_anzahl: number | null
  }

  return ((data ?? []) as unknown as Row[])
    .filter((sv): sv is Row & { standort_lat: number; standort_lng: number } =>
      typeof sv.standort_lat === 'number' && typeof sv.standort_lng === 'number',
    )
    .map((sv) => ({
      id: sv.id,
      vorname: sv.vorname,
      nachname: sv.nachname,
      firma_name: sv.firma_name,
      paket: sv.paket,
      stadt: sv.stadt,
      spezifikationen_top3: Array.isArray(sv.spezifikationen_top3) ? sv.spezifikationen_top3.slice(0, 3) : [],
      bewertungs_durchschnitt: sv.bewertungs_durchschnitt,
      bewertungs_anzahl: sv.bewertungs_anzahl,
      lat: sv.standort_lat,
      lng: sv.standort_lng,
    }))
}
```

- [ ] **Step 2: Typecheck**

Run:
```
npm run typecheck
```
Expected: grün. Falls Supabase-Types `spezifikationen_top3` o.ä. unbekannt sind, der `as unknown as Row[]`-Cast schützt schon davor.

- [ ] **Step 3: Commit**

```
git add src/lib/dispatch/karte/get-active-svs.ts
git commit -m "feat(AAR-912): getActiveSVs — aktive SVs mit standort_lat/lng"
```

---

## Task 4: `getTermineToday` Server-Loader

**Files:**
- Create: `src/lib/dispatch/karte/get-termine-today.ts`

- [ ] **Step 1: Loader implementieren**

```typescript
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/lib/supabase/database.types'
import type { PlzGeoRow, RawTerminForKarte, TerminPin, UnlocalizedLead } from './types'
import { resolveTerminGeo } from './resolve-termin-geo'

export async function getTermineToday(
  supabase: SupabaseClient<Database>,
  plzMap: Map<string, PlzGeoRow>,
): Promise<{ pins: TerminPin[]; unlocalized: UnlocalizedLead[] }> {
  const now = new Date()
  const startOfDay = new Date(now.getFullYear(), now.getMonth(), now.getDate(), 0, 0, 0).toISOString()
  const endOfDay = new Date(now.getFullYear(), now.getMonth(), now.getDate(), 23, 59, 59).toISOString()

  // Lift termine + joins. Wir nehmen das eingebettete-Join-Pattern von Supabase:
  // `leads(besichtigungsort_lat, ...)` und `sachverstaendige(standort_lat, ...)`.
  const { data, error } = await supabase
    .from('gutachter_termine')
    .select(
      `id, start_zeit, status, fall_id, lead_id, sv_id,
       gps_lat_ankunft, gps_lng_ankunft,
       lead:leads(vorname, nachname, besichtigungsort_lat, besichtigungsort_lng, kunde_plz, halter_plz),
       sv:sachverstaendige(vorname, nachname, standort_lat, standort_lng),
       fall:faelle(fall_nummer)` as 'id, start_zeit, status, fall_id, lead_id, sv_id, gps_lat_ankunft, gps_lng_ankunft',
    )
    .gte('start_zeit', startOfDay)
    .lte('start_zeit', endOfDay)
    .limit(500)

  if (error) {
    console.error('[karte] gutachter_termine query failed', error)
    return { pins: [], unlocalized: [] }
  }

  type EmbeddedLead = { vorname: string | null; nachname: string | null; besichtigungsort_lat: number | null; besichtigungsort_lng: number | null; kunde_plz: string | null; halter_plz: string | null }
  type EmbeddedSv = { vorname: string | null; nachname: string | null; standort_lat: number | null; standort_lng: number | null }
  type EmbeddedFall = { fall_nummer: string | null }
  type Row = {
    id: string
    start_zeit: string
    status: string | null
    fall_id: string | null
    lead_id: string | null
    sv_id: string | null
    gps_lat_ankunft: number | null
    gps_lng_ankunft: number | null
    lead?: EmbeddedLead | EmbeddedLead[] | null
    sv?: EmbeddedSv | EmbeddedSv[] | null
    fall?: EmbeddedFall | EmbeddedFall[] | null
  }

  const pins: TerminPin[] = []
  const unlocalized: UnlocalizedLead[] = []

  for (const raw of ((data ?? []) as unknown as Row[])) {
    const lead = Array.isArray(raw.lead) ? raw.lead[0] : raw.lead
    const sv = Array.isArray(raw.sv) ? raw.sv[0] : raw.sv
    const fall = Array.isArray(raw.fall) ? raw.fall[0] : raw.fall

    const flat: RawTerminForKarte = {
      id: raw.id,
      start_zeit: raw.start_zeit,
      status: raw.status,
      fall_id: raw.fall_id,
      lead_id: raw.lead_id,
      sv_id: raw.sv_id,
      gps_lat_ankunft: raw.gps_lat_ankunft,
      gps_lng_ankunft: raw.gps_lng_ankunft,
      lead_lat: lead?.besichtigungsort_lat ?? null,
      lead_lng: lead?.besichtigungsort_lng ?? null,
      lead_vorname: lead?.vorname ?? null,
      lead_nachname: lead?.nachname ?? null,
      sv_lat: sv?.standort_lat ?? null,
      sv_lng: sv?.standort_lng ?? null,
      sv_vorname: sv?.vorname ?? null,
      sv_nachname: sv?.nachname ?? null,
      fall_nummer: fall?.fall_nummer ?? null,
    }

    const leadPlz = lead?.kunde_plz ?? lead?.halter_plz ?? null
    const res = resolveTerminGeo(flat, plzMap, leadPlz)
    if (res.kind === 'pin') pins.push(res.pin)
    else unlocalized.push(res.lead)
  }

  return { pins, unlocalized }
}
```

- [ ] **Step 2: Typecheck**

Run:
```
npm run typecheck
```
Expected: grün.

- [ ] **Step 3: Commit**

```
git add src/lib/dispatch/karte/get-termine-today.ts
git commit -m "feat(AAR-912): getTermineToday mit Position-Priority-Chain"
```

---

## Task 5: `getKarteSnapshot` Combined Loader + Action-Update

**Files:**
- Create: `src/lib/dispatch/karte/get-karte-snapshot.ts`
- Modify: `src/app/dispatch/karte/actions.ts`
- Modify: `src/app/dispatch/karte/page.tsx`

- [ ] **Step 1: Combined Loader**

`src/lib/dispatch/karte/get-karte-snapshot.ts`:
```typescript
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/lib/supabase/database.types'
import type { KarteSnapshot, PlzGeoRow } from './types'
import { getTriageLeads } from './triage-leads'
import { getActiveSVs } from './get-active-svs'
import { getTermineToday } from './get-termine-today'

export async function getKarteSnapshot(
  supabase: SupabaseClient<Database>,
): Promise<KarteSnapshot> {
  // PLZ-Map einmal lesen, beide Loader teilen sie.
  type PlzRowWithOrt = { plz: string; lat: number; lng: number; ort?: string | null }
  const { data: plzRows } = await supabase
    .from('plz_geo')
    .select('plz, lat, lng, ort' as 'plz, lat, lng')

  const plzMap = new Map<string, PlzGeoRow>(
    ((plzRows ?? []) as unknown as PlzRowWithOrt[]).map((r) => [
      r.plz,
      { plz: r.plz, lat: Number(r.lat), lng: Number(r.lng), ort: r.ort ?? null },
    ]),
  )

  const [leadsSnapshot, svs, termineResult] = await Promise.all([
    getTriageLeads(supabase),
    getActiveSVs(supabase),
    getTermineToday(supabase, plzMap),
  ])

  return {
    leads: leadsSnapshot.pins,
    svs,
    termine: termineResult.pins,
    unlocalized: [...leadsSnapshot.unlocalized, ...termineResult.unlocalized],
  }
}
```

- [ ] **Step 2: Action umstellen**

Ersetze Inhalt von `src/app/dispatch/karte/actions.ts`:
```typescript
'use server'

import { createClient } from '@/lib/supabase/server'
import { getKarteSnapshot } from '@/lib/dispatch/karte/get-karte-snapshot'
import { requirePortalAccess } from '@/lib/auth/portal-guard'
import type { KarteSnapshot } from '@/lib/dispatch/karte/types'

export async function refetchKarteSnapshot(): Promise<
  { ok: true; data: KarteSnapshot } | { ok: false; error: string }
> {
  try {
    await requirePortalAccess(['dispatch', 'admin'])
  } catch {
    return { ok: false, error: 'unauthorized' }
  }
  const supabase = await createClient()
  const data = await getKarteSnapshot(supabase)
  return { ok: true, data }
}
```

- [ ] **Step 3: Page umstellen**

Ersetze `src/app/dispatch/karte/page.tsx`:
```tsx
import { createClient } from '@/lib/supabase/server'
import { getKarteSnapshot } from '@/lib/dispatch/karte/get-karte-snapshot'
import DispatchKarteClient from './DispatchKarteClient'

export const dynamic = 'force-dynamic'

export default async function DispatchKartePage() {
  const supabase = await createClient()
  const snapshot = await getKarteSnapshot(supabase)
  return (
    <div className="h-full w-full">
      <DispatchKarteClient initialSnapshot={snapshot} />
    </div>
  )
}
```

- [ ] **Step 4: Typecheck**

Run:
```
npm run typecheck
```
Expected: grün (DispatchKarteClient wird im nächsten Tasks-Block angepasst — bis dahin nimmt es den alten Snapshot-Typ).

Falls Fehler in DispatchKarteClient: temporär die `initialSnapshot`-Prop auf `any` casten:
```typescript
export default function DispatchKarteClient({ initialSnapshot }: { initialSnapshot: any }) {
```
Wird in Task 9 sauber typisiert.

- [ ] **Step 5: Commit**

```
git add src/lib/dispatch/karte/get-karte-snapshot.ts src/app/dispatch/karte/actions.ts src/app/dispatch/karte/page.tsx
git commit -m "feat(AAR-912): getKarteSnapshot + refetchKarteSnapshot Server-Action"
```

---

## Task 6: SVPopup Komponente

**Files:**
- Create: `src/app/dispatch/karte/SVPopup.tsx`

- [ ] **Step 1: Komponente schreiben**

```tsx
'use client'

import Link from 'next/link'
import type { SVPin } from '@/lib/dispatch/karte/types'

function svName(pin: SVPin): string {
  if (pin.firma_name) return pin.firma_name
  const parts = [pin.vorname, pin.nachname].filter(Boolean)
  return parts.length ? parts.join(' ') : 'Sachverständiger'
}

export default function SVPopup({ pin }: { pin: SVPin }) {
  const sterne = pin.bewertungs_durchschnitt
    ? `★ ${pin.bewertungs_durchschnitt.toFixed(1)} (${pin.bewertungs_anzahl ?? 0})`
    : null

  return (
    <div className="min-w-[240px] text-claimondo-navy">
      <div className="text-sm font-semibold">{svName(pin)}</div>
      <div className="mt-1 text-xs text-claimondo-navy/70">
        {pin.stadt ?? 'Standort unbekannt'}
        {pin.paket ? ` · ${pin.paket}` : null}
      </div>
      {sterne ? (
        <div className="mt-1 text-[11px] text-claimondo-shield font-medium">{sterne}</div>
      ) : null}
      {pin.spezifikationen_top3.length > 0 ? (
        <div className="mt-2 flex flex-wrap gap-1">
          {pin.spezifikationen_top3.map((s) => (
            <span
              key={s}
              className="rounded-ios-sm bg-claimondo-bg px-2 py-0.5 text-[10px] font-medium text-claimondo-ondo"
            >
              {s}
            </span>
          ))}
        </div>
      ) : null}
      <div className="mt-3 flex gap-2">
        <Link
          href={`/dispatch/sachverstaendige/${pin.id}`}
          className="rounded-ios-sm border border-claimondo-border bg-claimondo-bg px-3 py-1.5 text-xs font-medium text-claimondo-navy hover:bg-claimondo-border"
        >
          Details
        </Link>
        <Link
          href={`/dispatch/kalender?sv_id=${pin.id}&mode=create`}
          className="rounded-ios-sm bg-claimondo-navy px-3 py-1.5 text-xs font-medium text-white hover:bg-claimondo-navy/90"
        >
          Termin einplanen
        </Link>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Typecheck**

Run:
```
npm run typecheck
```
Expected: grün.

- [ ] **Step 3: Commit**

```
git add src/app/dispatch/karte/SVPopup.tsx
git commit -m "feat(AAR-912): SVPopup mit Details + Termin-einplanen CTAs"
```

---

## Task 7: TerminPopup Komponente

**Files:**
- Create: `src/app/dispatch/karte/TerminPopup.tsx`

- [ ] **Step 1: Komponente schreiben**

```tsx
'use client'

import Link from 'next/link'
import type { TerminPin } from '@/lib/dispatch/karte/types'

function statusLabel(status: string): string {
  switch (status) {
    case 'vorgeschlagen': return 'Vorgeschlagen'
    case 'bestaetigt': return 'Bestätigt'
    case 'sv_unterwegs': return 'SV unterwegs'
    case 'sv_angekommen': return 'SV vor Ort'
    case 'abgeschlossen': return 'Abgeschlossen'
    case 'abgesagt': return 'Abgesagt'
    case 'no_show': return 'No-Show'
    default: return status
  }
}

function uhrzeit(iso: string): string {
  return new Date(iso).toLocaleTimeString('de-DE', { hour: '2-digit', minute: '2-digit' })
}

export default function TerminPopup({ pin }: { pin: TerminPin }) {
  const detailHref = pin.fall_id
    ? `/admin/faelle/${pin.fall_id}`
    : pin.lead_id
      ? `/dispatch/leads/${pin.lead_id}`
      : null

  return (
    <div className="min-w-[220px] text-claimondo-navy">
      <div className="text-sm font-semibold">{uhrzeit(pin.start_zeit)} · {pin.kunde_name ?? 'Termin'}</div>
      <div className="mt-1 text-xs text-claimondo-navy/70">
        {pin.fall_nummer ?? '—'}
        {pin.sv_initialen ? ` · SV ${pin.sv_initialen}` : null}
      </div>
      <div className="mt-1 text-[11px] uppercase tracking-wide text-claimondo-shield">
        {statusLabel(pin.status)}
      </div>
      {detailHref ? (
        <Link
          href={detailHref}
          className="mt-3 inline-block rounded-ios-sm bg-claimondo-navy px-3 py-1.5 text-xs font-medium text-white hover:bg-claimondo-navy/90"
        >
          Fall öffnen
        </Link>
      ) : null}
    </div>
  )
}
```

- [ ] **Step 2: Typecheck**

Run:
```
npm run typecheck
```
Expected: grün.

- [ ] **Step 3: Commit**

```
git add src/app/dispatch/karte/TerminPopup.tsx
git commit -m "feat(AAR-912): TerminPopup mit Zeit/Status/Kunde/Fall-Link"
```

---

## Task 8: LayerChipBar Komponente

**Files:**
- Create: `src/app/dispatch/karte/LayerChipBar.tsx`

- [ ] **Step 1: Komponente schreiben**

```tsx
'use client'

import type { LayerKey } from '@/lib/dispatch/karte/types'

type Props = {
  visibility: Record<LayerKey, boolean>
  counts: Record<LayerKey, number>
  onToggle: (key: LayerKey) => void
}

const CHIPS: Array<{ key: LayerKey; label: string; emoji: string }> = [
  { key: 'leads', label: 'Leads', emoji: '🧑' },
  { key: 'svs', label: 'SVs', emoji: '🛠' },
  { key: 'termine', label: 'Termine', emoji: '📅' },
]

export default function LayerChipBar({ visibility, counts, onToggle }: Props) {
  return (
    <div className="absolute left-3 top-3 z-20 flex gap-2">
      {CHIPS.map(({ key, label, emoji }) => {
        const active = visibility[key]
        return (
          <button
            key={key}
            type="button"
            onClick={() => onToggle(key)}
            aria-pressed={active}
            className={`flex items-center gap-1.5 rounded-ios-md px-3 py-1.5 text-xs font-medium shadow-ios-sm transition-colors ${
              active
                ? 'bg-claimondo-navy text-white hover:bg-claimondo-navy/90'
                : 'bg-white/95 text-claimondo-navy/60 hover:bg-claimondo-bg backdrop-blur'
            }`}
          >
            <span aria-hidden>{emoji}</span>
            <span>{label}</span>
            <span className={`rounded-ios-sm px-1.5 py-0.5 text-[10px] ${active ? 'bg-white/20' : 'bg-claimondo-bg'}`}>
              {counts[key]}
            </span>
          </button>
        )
      })}
    </div>
  )
}
```

- [ ] **Step 2: Typecheck**

Run:
```
npm run typecheck
```
Expected: grün.

- [ ] **Step 3: Commit**

```
git add src/app/dispatch/karte/LayerChipBar.tsx
git commit -m "feat(AAR-912): LayerChipBar Floating-Toggle für die 3 Layer"
```

---

## Task 9: DispatchKarteClient — Source/Layer-Rewrite + LayerChipBar + Popups

**Files:**
- Modify: `src/app/dispatch/karte/DispatchKarteClient.tsx`

Hauptarbeit. v1 nutzte `mapboxgl.Marker` pro Pin — für Cluster brauchen wir `addSource({type:'geojson', cluster:true}) + addLayer()`. Marker-basiertes Klick-Handling weicht einem `map.on('click', layerId, ...)`-Pattern.

- [ ] **Step 1: Komponente komplett ersetzen**

```tsx
'use client'

import { useCallback, useEffect, useRef, useState } from 'react'
import { createRoot, type Root } from 'react-dom/client'
import { ensureMapboxInitialized, mapboxgl } from '@/lib/mapbox/client'
import type { Map as MapboxMap, Popup, MapMouseEvent, GeoJSONSource } from 'mapbox-gl'
import type {
  KarteSnapshot,
  LayerKey,
  SVPin,
  TerminPin,
  TriageLeadPin,
} from '@/lib/dispatch/karte/types'
import LeadPopup from './LeadPopup'
import SVPopup from './SVPopup'
import TerminPopup from './TerminPopup'
import LayerChipBar from './LayerChipBar'
import UnlocalizedSidebar from './UnlocalizedSidebar'
import { useTriageRealtime } from './useTriageRealtime'
import { refetchKarteSnapshot } from './actions'

const DEFAULT_CENTER: [number, number] = [10.45, 51.16]
const DEFAULT_ZOOM = 5.4

// AAR-912: Source-IDs pro Layer
const SRC = { leads: 'src-leads', svs: 'src-svs', termine: 'src-termine' } as const
const LAYER_POINT = { leads: 'lay-leads-pt', svs: 'lay-svs-pt', termine: 'lay-termine-pt' } as const
const LAYER_CLUSTER = { leads: 'lay-leads-cl', svs: 'lay-svs-cl', termine: 'lay-termine-cl' } as const
const LAYER_CLUSTER_COUNT = {
  leads: 'lay-leads-clc',
  svs: 'lay-svs-clc',
  termine: 'lay-termine-clc',
} as const

function leadsToGeoJSON(pins: TriageLeadPin[]): GeoJSON.FeatureCollection {
  return {
    type: 'FeatureCollection',
    features: pins.map((p) => ({
      type: 'Feature',
      id: p.id,
      geometry: { type: 'Point', coordinates: [p.lng, p.lat] },
      properties: { __pinType: 'lead', __id: p.id, geoSource: p.geoSource },
    })),
  }
}
function svsToGeoJSON(pins: SVPin[]): GeoJSON.FeatureCollection {
  return {
    type: 'FeatureCollection',
    features: pins.map((p) => ({
      type: 'Feature',
      id: p.id,
      geometry: { type: 'Point', coordinates: [p.lng, p.lat] },
      properties: { __pinType: 'sv', __id: p.id },
    })),
  }
}
function termineToGeoJSON(pins: TerminPin[]): GeoJSON.FeatureCollection {
  return {
    type: 'FeatureCollection',
    features: pins.map((p) => ({
      type: 'Feature',
      id: p.id,
      geometry: { type: 'Point', coordinates: [p.lng, p.lat] },
      properties: { __pinType: 'termine', __id: p.id, status: p.status },
    })),
  }
}

const TERMIN_STATUS_COLOR: Record<string, string> = {
  vorgeschlagen: '#F59E0B',
  bestaetigt: '#10B981',
  sv_unterwegs: 'var(--claimondo-shield, #1E3A5F)',
  sv_angekommen: 'var(--claimondo-shield, #1E3A5F)',
}

export default function DispatchKarteClient({
  initialSnapshot,
}: {
  initialSnapshot: KarteSnapshot
}) {
  const containerRef = useRef<HTMLDivElement | null>(null)
  const mapRef = useRef<MapboxMap | null>(null)
  const popupRef = useRef<Popup | null>(null)
  const popupRootRef = useRef<Root | null>(null)
  const [snapshot, setSnapshot] = useState<KarteSnapshot>(initialSnapshot)
  const [visibility, setVisibility] = useState<Record<LayerKey, boolean>>({
    leads: true,
    svs: true,
    termine: true,
  })
  const [tokenOk, setTokenOk] = useState<boolean>(true)
  const initialFitDoneRef = useRef(false)

  // --- Map mount ---
  useEffect(() => {
    if (!containerRef.current) return
    const ok = ensureMapboxInitialized()
    setTokenOk(ok)
    if (!ok) return

    const styleUrl =
      process.env.NEXT_PUBLIC_MAPBOX_STYLE_URL || 'mapbox://styles/mapbox/light-v11'

    const map = new mapboxgl.Map({
      container: containerRef.current,
      style: styleUrl,
      center: DEFAULT_CENTER,
      zoom: DEFAULT_ZOOM,
      attributionControl: false,
    })
    map.addControl(new mapboxgl.NavigationControl({ visualizePitch: false }), 'top-right')

    map.on('load', () => {
      // Sources + Layers für alle 3 Pin-Kategorien
      ;(['leads', 'svs', 'termine'] as LayerKey[]).forEach((key) => {
        map.addSource(SRC[key], {
          type: 'geojson',
          data: { type: 'FeatureCollection', features: [] },
          cluster: true,
          clusterMaxZoom: 8,
          clusterRadius: 50,
        })

        // Cluster-Circle
        map.addLayer({
          id: LAYER_CLUSTER[key],
          type: 'circle',
          source: SRC[key],
          filter: ['has', 'point_count'],
          paint: {
            'circle-color':
              key === 'svs'
                ? 'rgba(30,58,95,0.85)'
                : key === 'termine'
                  ? 'rgba(245,158,11,0.85)'
                  : 'rgba(13,27,62,0.85)',
            'circle-radius': ['step', ['get', 'point_count'], 18, 10, 24, 50, 32],
            'circle-stroke-color': '#ffffff',
            'circle-stroke-width': 2,
          },
        })

        // Cluster-Count-Label
        map.addLayer({
          id: LAYER_CLUSTER_COUNT[key],
          type: 'symbol',
          source: SRC[key],
          filter: ['has', 'point_count'],
          layout: {
            'text-field': '{point_count_abbreviated}',
            'text-size': 12,
            'text-font': ['Open Sans Bold', 'Arial Unicode MS Bold'],
          },
          paint: { 'text-color': '#ffffff' },
        })

        // Einzelpunkte (zoom > clusterMaxZoom oder isolated)
        map.addLayer({
          id: LAYER_POINT[key],
          type: 'circle',
          source: SRC[key],
          filter: ['!', ['has', 'point_count']],
          paint: {
            'circle-color':
              key === 'leads'
                ? ['case',
                    ['==', ['get', 'geoSource'], 'plz_centroid'],
                    'rgba(123,163,204,1)',
                    'rgba(13,27,62,1)',
                  ]
                : key === 'svs'
                  ? 'rgba(30,58,95,1)'
                  : ['match', ['get', 'status'],
                      'vorgeschlagen', '#F59E0B',
                      'bestaetigt', '#10B981',
                      'sv_unterwegs', '#1E3A5F',
                      'sv_angekommen', '#1E3A5F',
                      'rgba(69,115,162,1)',
                    ],
            'circle-radius': key === 'svs' ? 9 : 7,
            'circle-stroke-color': '#ffffff',
            'circle-stroke-width': 2,
          },
        })

        // Klick auf einzelnen Pin → Popup
        map.on('click', LAYER_POINT[key], (e: MapMouseEvent & { features?: mapboxgl.MapboxGeoJSONFeature[] }) => {
          const feature = e.features?.[0]
          if (!feature) return
          const id = feature.properties?.__id as string
          const type = feature.properties?.__pinType as string
          const coords = (feature.geometry as GeoJSON.Point).coordinates as [number, number]
          openPopup(map, coords, type as LayerKey, id)
        })

        // Klick auf Cluster → reinzoomen
        map.on('click', LAYER_CLUSTER[key], (e: MapMouseEvent & { features?: mapboxgl.MapboxGeoJSONFeature[] }) => {
          const feature = e.features?.[0]
          if (!feature) return
          const clusterId = feature.properties?.cluster_id as number
          const source = map.getSource(SRC[key]) as GeoJSONSource
          source.getClusterExpansionZoom(clusterId).then((zoom: number) => {
            const coords = (feature.geometry as GeoJSON.Point).coordinates as [number, number]
            map.easeTo({ center: coords, zoom })
          })
        })

        // Hover-Cursor
        map.on('mouseenter', LAYER_POINT[key], () => { map.getCanvas().style.cursor = 'pointer' })
        map.on('mouseleave', LAYER_POINT[key], () => { map.getCanvas().style.cursor = '' })
        map.on('mouseenter', LAYER_CLUSTER[key], () => { map.getCanvas().style.cursor = 'pointer' })
        map.on('mouseleave', LAYER_CLUSTER[key], () => { map.getCanvas().style.cursor = '' })
      })

      // initial Daten laden
      pushDataToMap(map, snapshot)
      // initial fit
      if (!initialFitDoneRef.current) doFitBounds(map, snapshot)
    })

    mapRef.current = map
    return () => {
      popupRef.current?.remove()
      popupRootRef.current?.unmount()
      map.remove()
      mapRef.current = null
      initialFitDoneRef.current = false
    }
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [])

  // --- Daten in Sources schreiben bei Snapshot-Änderung ---
  useEffect(() => {
    const map = mapRef.current
    if (!map || !map.isStyleLoaded()) return
    pushDataToMap(map, snapshot)
  }, [snapshot])

  // --- Visibility Toggle ---
  useEffect(() => {
    const map = mapRef.current
    if (!map || !map.isStyleLoaded()) return
    ;(['leads', 'svs', 'termine'] as LayerKey[]).forEach((key) => {
      const vis = visibility[key] ? 'visible' : 'none'
      ;[LAYER_POINT[key], LAYER_CLUSTER[key], LAYER_CLUSTER_COUNT[key]].forEach((layerId) => {
        if (map.getLayer(layerId)) map.setLayoutProperty(layerId, 'visibility', vis)
      })
    })
  }, [visibility])

  // --- Popup-Logik ---
  const openPopup = useCallback(
    (map: MapboxMap, coords: [number, number], type: LayerKey, id: string) => {
      popupRef.current?.remove()
      popupRootRef.current?.unmount()

      const container = document.createElement('div')
      const root = createRoot(container)

      if (type === 'leads') {
        const pin = snapshot.leads.find((p) => p.id === id)
        if (!pin) return
        root.render(<LeadPopup pin={pin} />)
      } else if (type === 'svs') {
        const pin = snapshot.svs.find((p) => p.id === id)
        if (!pin) return
        root.render(<SVPopup pin={pin} />)
      } else {
        const pin = snapshot.termine.find((p) => p.id === id)
        if (!pin) return
        root.render(<TerminPopup pin={pin} />)
      }

      const popup = new mapboxgl.Popup({ offset: 12, closeButton: true })
        .setLngLat(coords)
        .setDOMContent(container)
        .addTo(map)

      popup.on('close', () => {
        root.unmount()
        if (popupRef.current === popup) popupRef.current = null
      })

      popupRef.current = popup
      popupRootRef.current = root
    },
    [snapshot],
  )

  const onToggle = useCallback((key: LayerKey) => {
    setVisibility((prev) => ({ ...prev, [key]: !prev[key] }))
  }, [])

  // --- Realtime ---
  const refetch = useCallback(async () => {
    const result = await refetchKarteSnapshot()
    if (result.ok) setSnapshot(result.data)
  }, [])
  useTriageRealtime(refetch)

  if (!tokenOk) {
    return (
      <div className="flex h-full items-center justify-center text-claimondo-navy">
        Mapbox-Token fehlt — Karte kann nicht initialisiert werden.
      </div>
    )
  }

  const counts: Record<LayerKey, number> = {
    leads: snapshot.leads.length,
    svs: snapshot.svs.length,
    termine: snapshot.termine.length,
  }

  return (
    <div className="relative h-full w-full">
      <div ref={containerRef} className="h-full w-full" />
      <LayerChipBar visibility={visibility} counts={counts} onToggle={onToggle} />
      <UnlocalizedSidebar leads={snapshot.unlocalized} />
    </div>
  )
}

// --- Helper: Daten in alle Sources schreiben ---
function pushDataToMap(map: MapboxMap, snapshot: KarteSnapshot) {
  const srcLeads = map.getSource(SRC.leads) as GeoJSONSource | undefined
  const srcSvs = map.getSource(SRC.svs) as GeoJSONSource | undefined
  const srcTermine = map.getSource(SRC.termine) as GeoJSONSource | undefined
  srcLeads?.setData(leadsToGeoJSON(snapshot.leads))
  srcSvs?.setData(svsToGeoJSON(snapshot.svs))
  srcTermine?.setData(termineToGeoJSON(snapshot.termine))
}

// --- Helper: fitBounds einmal beim ersten Render mit Daten ---
function doFitBounds(map: MapboxMap, snapshot: KarteSnapshot) {
  const all = [...snapshot.leads, ...snapshot.svs, ...snapshot.termine]
  if (all.length === 0) return
  const bounds = new mapboxgl.LngLatBounds()
  all.forEach((p) => bounds.extend([p.lng, p.lat]))
  map.fitBounds(bounds, { padding: 80, maxZoom: 10, duration: 0 })
}
```

- [ ] **Step 2: Typecheck**

Run:
```
npm run typecheck
```
Expected: grün. Eventuelle Mapbox-Typ-Issues mit `MapMouseEvent.features`-Field: per `as`-Cast lösen (siehe Code oben).

- [ ] **Step 3: Build**

Run:
```
npm run build
```
Expected: grün.

- [ ] **Step 4: Commit**

```
git add src/app/dispatch/karte/DispatchKarteClient.tsx
git commit -m "feat(AAR-912): DispatchKarteClient v2 — GeoJSON-Sources + Cluster + 3 Layer"
```

---

## Task 10: useTriageRealtime erweitern

**Files:**
- Modify: `src/app/dispatch/karte/useTriageRealtime.ts`

- [ ] **Step 1: Subscriptions ergänzen**

Vorhandener Hook hört bereits auf `leads` + `auftraege`. Wir ergänzen `gutachter_termine` + `sachverstaendige`.

```typescript
'use client'

import { useEffect, useId } from 'react'
import { createClient } from '@/lib/supabase/client'

export function useTriageRealtime(onChange: () => void): void {
  const instanceId = useId()
  useEffect(() => {
    const supabase = createClient()
    const channelName = `dispatch-karte-${instanceId}`
    const channel = supabase
      .channel(channelName)
      .on('postgres_changes', { event: '*', schema: 'public', table: 'leads' }, () => onChange())
      .on('postgres_changes', { event: '*', schema: 'public', table: 'auftraege' }, () => onChange())
      .on('postgres_changes', { event: '*', schema: 'public', table: 'gutachter_termine' }, () => onChange())
      .on('postgres_changes', { event: '*', schema: 'public', table: 'sachverstaendige' }, () => onChange())
      .subscribe()
    return () => {
      supabase.removeChannel(channel)
    }
  }, [instanceId, onChange])
}
```

- [ ] **Step 2: Typecheck**

Run:
```
npm run typecheck
```
Expected: grün.

- [ ] **Step 3: Commit**

```
git add src/app/dispatch/karte/useTriageRealtime.ts
git commit -m "feat(AAR-912): Realtime auf gutachter_termine + sachverstaendige erweitern"
```

---

## Task 11: Tests grün + Build + Audit + Push + PR

**Files:** keine Code-Änderung.

- [ ] **Step 1: Tests**

```
npm run test -- resolve-termin-geo triage-leads
```
Expected: alle PASS (7 resolve-termin-geo + 7 triage-leads aus v1).

- [ ] **Step 2: Full Build**

```
NODE_OPTIONS='--max-old-space-size=8192' npm run build
```
Expected: grün, `/dispatch/karte` in der Routes-Liste.

- [ ] **Step 3: Token-Audit (alle 3 Ratchets)**

```
npm run check:token-audit
```
Expected: 0 Hex-Verstöße, Radii-Baseline gleich oder gesenkt, Accent-Baseline gleich oder gesenkt.

- [ ] **Step 4: Push + PR**

```
git push -u origin kitta/aar-912-karte-v2
gh pr create --base staging --title "feat(AAR-912): Dispatcher-Karte v2 — SVs + Termine + LayerChipBar + Cluster" --body "$(cat <<'EOF'
## Summary

v2 der Dispatcher-Karte (Folge zu AAR-894 v1):

- **SVs-Layer** — aktive Sachverständige als Pins (`sachverstaendige.standort_lat/lng`)
- **Termine-Layer** — heutige Termine, Status-Farbcodiert (vorgeschlagen=amber, bestaetigt=emerald, sv_unterwegs/angekommen=brand-shield)
- **LayerChipBar** — Floating-Chips oben links, toggelt jeden Layer einzeln
- **Mapbox-Cluster** — `cluster:true` pro Source, max-zoom 8, radius 50px

Architektur: GeoJSON-Sources + Layers ersetzen Marker-Rendering. Cluster-Click zoomt ein. Pin-Click rendert React-Popup via `createRoot`.

Realtime: erweitert auf `gutachter_termine` + `sachverstaendige` (zusätzlich zu `leads`/`auftraege` aus v1).

Whitelabel-fähig via AAR-906 `--map-pin-*`-CSS-Vars.

## Akzeptanzkriterien

- [x] 3 Pin-Layer mit korrekten Counts
- [x] LayerChipBar oben links, Klick toggelt Visibility
- [x] SV-Popup mit Details-CTA + Termin-einplanen-CTA
- [x] Termin-Popup mit Status + Fall-Detail-Link
- [x] Cluster ab Zoom 8 + darunter, Click-to-zoom
- [x] Realtime-Subs auf 4 Tabellen
- [x] Brand-Hex-Audit + Radii-Ratchet + Accent-Ratchet grün

Linear: AAR-912
Spec: docs/superpowers/specs/2026-05-14-dispatcher-karte-v2-design.md

🤖 Generated with Claude Code
EOF
)"
```

- [ ] **Step 5: Linear updaten**

Linear AAR-912 → Status `In Review` + PR-Link.

---

## Out of Scope (v3 — separate Tickets)

- Self-Dispatch-Anfragen-Layer (4. Pin-Set)
- Awareness/Triage-Preset-Modi
- Drag-to-assign-SV direkt aus Karte
- Live-GPS-Tracking-Layer (SV-Position alle 30s)
- Heatmap-Layer (Lead-Dichte)
- Geo-Backfill-Job für Bestands-Leads ohne lat/lng
