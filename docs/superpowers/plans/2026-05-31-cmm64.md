# CMM-64 — Umsetzungsplan (Vorschäden/Cardentity)

Spec: `docs/superpowers/specs/2026-05-31-cmm64-vorschaeden-cardentity.md`
Branch: `kitta/cmm-ssot-strecke-31-05` (Worktree, off origin/staging)

---

## PR1 — Schema-Struktur (additiv) — DIESE SESSION

**Gate-Bedingung:** rein additiv, 0 Live-Daten, kein src/-Change außer Migration-File + Docs. Per AGENTS.md Regel 2 via Supabase-Plugin.

1. [ ] DDL via `apply_migration({ name: "cmm64_pr1_vorschaeden_cardentity_homes", query: <DDL> })`.
2. [ ] `list_migrations` → getrackte Version `<V>` ablesen.
3. [ ] Migration-File committen als `supabase/migrations/<V>_cmm64_pr1_vorschaeden_cardentity_homes.sql` (Name == `<V>`).
4. [ ] `execute_sql` READ-Verifikation:
   - `vehicle_vorschaeden` existiert, RLS enabled, 2 Policies, FK ON DELETE CASCADE, updated_at-Trigger.
   - `vehicles.cardentity_report` + `claims.{hat_vorschaeden,vorschaden_geprueft,vorschaden_erkannt,vorschaeden_beschreibung}` existieren.
   - alle 6 faelle-Views resolven weiterhin (`pg_get_viewdef` ohne Fehler / SELECT count(*) je View).
5. [ ] `get_advisors(security)` → keine neue Warnung durch die neue Tabelle (RLS muss sie still halten).
6. [ ] Types: **aufgeschoben** auf PR2 (kein Consumer referenziert die Spalten in PR1 — Regel 2 Schritt 6 erlaubt das).
7. [ ] 7-Punkte-Audit im Commit-Body. PR gegen `staging`.

## PR2 — Writer (GATED, nach Review)
- typ-a/route.ts, typ-b.ts, enrich-fahrzeug.ts auf neues Modell. claims-Flags sofort, vehicle-Rows wenn vehicle_id da (non-fatal guard). Smoke Cardentity-Trigger.

## PR3 — Reader/View-Repoint (GATED, nach Review)
- v_claim_full + v_faelle_mit_aktuellem_termin (+ kunde/sv_view) Vorschaden-Felder claim/vehicle-zentrisch. EXCEPT-0/0 + Portal-Smoke. ~20 Reader-Sweep.

---

## Verifikations-Queries (PR1)
```sql
-- Tabelle + RLS + Policies
SELECT relrowsecurity FROM pg_class WHERE relname='vehicle_vorschaeden';
SELECT policyname FROM pg_policies WHERE tablename='vehicle_vorschaeden';
-- Neue Spalten
SELECT table_name, column_name FROM information_schema.columns
 WHERE (table_name='vehicles' AND column_name='cardentity_report')
    OR (table_name='claims' AND column_name IN ('hat_vorschaeden','vorschaden_geprueft','vorschaden_erkannt','vorschaeden_beschreibung'));
-- Views unverändert resolvbar
SELECT count(*) FROM v_claim_full; SELECT count(*) FROM v_faelle_mit_aktuellem_termin;
```
