# CMM-44 SP-I5 (Rüge 6 Spalten → kanzlei_faelle) Plan

> Slice 5 des SP-I-Clusters. Recipe-Quelle: `docs/23.05.2026/handoff-cmm44-sp-i-completion.md`. **SP-I6 (`kanzlei_id`) NICHT hier** — separater Slice (TBD-Verdikt + `.eq('kanzlei_id')`-Billing-Filter in erstelle-abrechnung).

**Goal:** 6 Rüge-Lifecycle-Spalten rein additiv von `faelle` auf `kanzlei_faelle` (1:1).

**Spalten (6):** `ruege_erhalten_am`/`ruege_gesendet_am` (tstz), `ruege_grund` (text), `ruege_betrag` (numeric(10,2)), `ruege_counter` (int DEFAULT 0), `ruege_frist_tage` (int DEFAULT 14).

## Drift-Check (Live 2026-05-23) — done
- Alle 6 nur auf `faelle`, kein kanzlei_faelle-Drift. cov: 4× 0; `ruege_counter` 49/49 (=Default 0), `ruege_frist_tage` 49/49 (=Default 14) → **kein Daten-Backfill**: kanzlei_faelle erbt dieselben Defaults (ADD DEFAULT) + Views COALESCEn (analog SP-I3 vs_eskalationsstufe).
- Nur **1 View** (`v_faelle_mit_aktuellem_termin`, kf-Join via SP-I1) exponiert die 6.

## PR1 — Schema (Migration `20260523200236`) — done
6 ADD COLUMN (counter/frist_tage mit DEFAULT 0/14) + 1 View-Repoint (6× f→kf; `ruege_betrag::numeric(10,2)`, `COALESCE(kf.ruege_counter,0)`, `COALESCE(kf.ruege_frist_tage,14)`), generiert via `scripts/_spi5-gen-views.mjs`. Live appliziert + repair. Types regeneriert (additiv 18+/0−).

## PR2 — Code-Sweep — done
- **Writer:** `KANZLEI_FAELLE_COLS` += 6 → `process-event.ts` (`ruege_1/2_gesendet`-Events setzen ruege_gesendet_am+ruege_counter in computeFieldUpdates) automatisch via Peel-Kaskade. Plus `_actions/prozess.ts` `startRuege` (liest prevCounter aus kanzlei_faelle-Embed, schreibt ruege_counter+ruege_gesendet_am via upsertKanzleiFall; updated_at bleibt faelle).
- **Reader:** `completion-signals.ts` (2× kanzlei_ruege_versand/kanzlei_kuerzung_antwort → `kanzlei_faelle(ruege_gesendet_am)`-Embed, status bleibt faelle-flach), `blocker-detection.ts` (ruege_gesendet_am in den bestehenden kanzlei_faelle-Embed, war ungenutzt). Display (Sections/Kunde) + section-visibility = Pattern E (View/Props).

## Verifikation
Voller `next build` grün (8GB-Heap). Re-Grep: 0 bare faelle-ruege-Reads. Smoke (`scripts/smoke-cmm44-spi5.mjs`) Post-Merge. **Rein additiv.** Danach offen: nur noch **SP-I6** `kanzlei_id` (1, Verdikt + Billing-Filter-Refactor mit Aaron).
