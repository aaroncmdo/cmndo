# W3.1 / AAR-952 — SLA-Triage „24/26 breached" (Personal-Cleanup AAR-943)

**Datum:** 2026-06-02 · **DB:** `paizkjajbuxxksdoycev` (Prod) · **Ergebnis: ENTWARNUNG — Test-Rauschen, kein Bug, keine Ops-Lücke.**

## Ausgangslage
`sla_tracking`: **24 breached · 2 pending · 0 completed** (Stand 02.06.). Das „0 completed" war der Verdachts-Trigger.

## Diagnose (live)
| Befund | Ergebnis |
|---|---|
| breached nach `sla_typ` | termin_bestaetigung 9 · gutachter_zuweisung 8 · besichtigung 7 — **alle `target_rolle='sv'`** |
| Eskalation gelaufen? | **alle 24 haben `eskalation_task_id`** (Breach-Erkennung + Task-Erstellung lief) |
| `work_state` der claims | **alle 24 → `dispatch_done`** (Gutachter disponiert, SV-Seite nie weiter) |
| Zeitfenster | 2026-05-13 bis 2026-05-31 |
| Test- vs Echtdaten | **100 % Test:** 9 distinct claims, alle `kunde_email` = Smoke-Marker (`smoke-multi-*@claimondo.test`, `smoke-voll-*`, `smoke-lokal-*`, `aaron.sprafke+kunde-*`, `smoke-aar940-*@example.com`) |

## SLA-Mechanik ist korrekt (kein fehlender Pfad)
`src/lib/sla/tracker.ts`: `startSla` (idempotent via UNIQUE(fall_id,sla_typ)) · `checkAndEscalateBreaches` (Cron: pending+breach_at<now → breached + Eskalations-Task + Timeline) · **`completeSla` existiert** (setzt `completed` für pending|breached bei Phasen-Übergang). Alle drei vorhanden + funktional.

## Schlussfolgerung
- Die 24 Breaches sind **legitim** auf abandoned **Smoke-Test-Claims**: Test-SVs haben die Aktionen, die `completeSla` auslösen würden (Zuweisung/Termin-Bestätigung/Besichtigung), nie ausgeführt → SLAs liefen ab → korrekt eskaliert → nie completed, weil die Cases abgebrochen wurden.
- **`0 completed` = pre-launch:** kein echter Case hat je eine SLA bis zum Abschluss durchlaufen; die einzigen SLA-Daten sind dieses Test-Rauschen.
- **Kein Cron-Über-Flagging, kein fehlender Completion-Pfad, keine echte Ops-Lücke.**

## Empfehlung
- **Kein Code-Change.** Die SLA-Pipeline ist gesund.
- Die 24 stale `sla_tracking`-Rows + die 9 Smoke-Claims im Rahmen von **W3.2 (AAR-953, tote Tische/Seeds)** mit-bereinigen (dort werden die Mai-Smoke-Seeds ohnehin angefasst). Bewusst NICHT hier separat gelöscht, um W3.2 nicht vorzugreifen.
- Nach Launch: SLA-Completion-Rate erneut prüfen — dann sollten echte Cases `completed`-Einträge erzeugen.
