# W2.3 / AAR-951 PR2 — profiles HR-Spalten droppen (Personal-Cleanup AAR-943)

**Datum:** 2026-06-02 · **DB:** `paizkjajbuxxksdoycev` (Prod) · **Migration:** `20260602092656_aar951_pr2_drop_profiles_hr_columns`

Drop von `profiles.{gehalt_brutto, gehaltsstufe, position, eingestellt_am}` — seit PR1 (#2214) leben sie in der admin-only `mitarbeiter_verguetung`.

## Gates (alle vor dem Drop verifiziert)
| Gate | Ergebnis |
|---|---|
| **PR1 #2214 prod-live** | ✅ VPS-Verify: Prod-Build `/var/www/claimondo-v2` mtime **2026-06-02 09:00 UTC**, liest `mitarbeiter_verguetung` in **5 ausführbaren SSR-`.js`-Chunks**; PM2-Reload 09:22 UTC. #2214 ist Ancestor von `origin/main`. |
| **Reader-Sweep** (`src/`) | ✅ Kein Code liest die 4 Spalten aus `profiles` — nur `/admin/team` (page/[id]/actions) via `mitarbeiter_verguetung`. `position`-Treffer sonst (mapbox/kanzlei/geo) sind keine profiles-Reads. |
| **Info-Loss-Gate** | ✅ Einziger Wert (`profile a97b63ee`, `eingestellt_am=2026-05-11`) ist identisch nach `mitarbeiter_verguetung` migriert (`in_mv=true`). `gehalt_brutto/gehaltsstufe/position` = 0 non-null. |
| **DB-Deps** | ✅ 0 Views/Matviews/Indexe auf den 4 Spalten. |
| **Post-Drop Prod-Error-Log** | ✅ `claimondo-v2-error.log` ohne `does not exist`/`column`/`42703`/`admin/team`-Fehler seit Drop. |

## Verifikation nach Drop
- DB: `profiles_hr_cols_remaining = 0`.
- `npm run build` **exit 0** (stale Types unkritisch — 0 Reader der gedroppten Spalten).
- **Post-Drop-Boot-Smoke** (staging, geteilte DB = post-drop): `login`/`admin`/**`admin-team`**/`admin-finance`/`sv-portal`/`kunde` → alle **HTTP 200, keine Error-Boundary**.

## Type-Handling (bewusst deferred)
Per W2.3-PR1-Lesson (`database.types.ts` = Multi-Session-Collision-Hotspot): **kein Full-Regen mitten in der parallelen Arbeit.** Daher offen für den nächsten koordinierten Regen:
- 4 Phantom-Spalten aus dem `profiles`-Typ entfernen,
- `mitarbeiter_verguetung`-Typ ergänzen,
- die 4 `any`-Casts in `admin/team` durch echte Typen ersetzen (tragen bereits `Type-Regen folgt`-Kommentare).

0 Reader der gedroppten Spalten → kein Runtime-Risiko, Build grün. Keine W2.3-PR3 nötig — PR2 schließt AAR-951 (PR1 Tabelle+Repoint, PR2 Drop).
