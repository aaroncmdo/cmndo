# Dead / stub / never-wired tables

No CMM-49 doc file in this worktree (it's the Linear master plan #2118, not a local file). My grep of `_specs/` already confirmed none of these tables appear there. The `coveredByCMM49` claims rest on the SHARED CONTEXT facts (which list `parteien` as a post-drop DEAD-READS cleanup surface and `faelle`-scoped writer migration in §C/§D), so I'll honor those. I have enough evidence. Writing the corrected report.

## Dead / Stub / Never-Wired Tables — Lifecycle & Schema Audit (Adversarially Verified)

**Summary:** I re-ran every key row-count and re-grepped every cited call site against live PROD (`paizkjajbuxxksdoycev`) and the worktree. The finder's core thesis holds up well and is unusually disciplined about distinguishing "0 rows" from "dead": the 11 truly-orphaned tables are confirmed (0 rows AND a properly `.from()`-bounded grep returns **zero** real call sites — the only repo hits are the baseline schema SQL and generated `database.types.ts`). The legacy-superseded live-writer findings (`termine`, `parteien`, `zahlungseingaenge`/`zahlungspositionen`) are all confirmed with exact file:line. Two corrections: (1) `cron_jobs_audit` is **24,491 rows** live, not "~24,486" — and critically `pg_stat_user_tables.n_live_tup` reports it as **559**, a fresh demonstration that the stale-stat caveat applies here too (n_live_tup is ~45x too low). (2) The finder under-counted `zahlungseingaenge` readers — there are two additional analytics readers it missed. `phase_transitions` is confirmed as a dual-writer, fall_id-keyed lifecycle log (1 row live), and I confirmed the second writer at `endzustand-actions.ts:79` is `fall_id`-keyed and `transitioned_by`-stamped. One severity adjustment: the truly-orphaned 11 are HIGH→**MEDIUM** (genuinely dead but zero blast-radius, pure inventory hygiene, not blocking anything). Net: no finding REFUTED, one ADJUSTED on numbers, one ADJUSTED on severity, several CONFIRMED.

### Findings

- **[MEDIUM] 11 truly-orphaned tables: 0 rows + 0 code refs (drop candidates)**  ·  category: dead-code  ·  effort: m  ·  coveredByCMM49: no  ·  **[ADJUSTED: severity HIGH→MEDIUM]**
  - Evidence: All 11 confirmed `count(*)=0` in one query (`communities`/`community_memberships`/`sv_community`/`sv_buero`/`sv_buero_memberships`/`sv_organisation`/`sv_organisation_memberships`/`sv_organisation_laeufer_reports`/`werkstaetten`/`vehicle_ownership_history`/`gutachten_fotos` = all 0). I tightened the finder's grep to a **`.from('<table>')` boundary** across the entire worktree (not just `src`): **No matches found**. A loose substring grep does surface 10 files, but I verified those are all noise — `vehicles`/`community_leaderboard` substring collisions (`src/lib/vehicles/ensure-vehicle.ts`, `src/app/admin/statistiken/*`), the baseline schema (`supabase/migrations/00000000000000_baseline_public_schema.sql`), and generated types (`src/lib/supabase/database.types.ts`, `claimondo-marketing/lib/supabase/database.types.ts`). Zero of these are real reads/writes. Also confirmed: none of the 11 appear in `_specs/` (the CMM-49 spec dir).
  - Recommendation: Unchanged from finder — two sub-groups. (a) The `sv_buero*`/`sv_organisation*`/`communit*`/`sv_community` 8-table org/community normalization is abandoned; the live org feature uses `organisationen` flat columns + `community_leaderboard`. (b) `werkstaetten`, `vehicle_ownership_history`, `gutachten_fotos` are individually superseded. DROP as confirmation-gated migration(s). I downgrade severity to MEDIUM because these block nothing and carry no runtime risk — they are pure schema-inventory clutter (a real cost at 149 tables / 3806 columns, but not urgent).
  - Info-loss risk: none for the 8 membership/community shells. `vehicle_ownership_history` is gated on CMM-50 (vehicles write-path never wired — `vehicles` itself has exactly **1 row**, confirmed) — park it, don't drop, until CMM-50 resolves.
  - Verdict: **[CONFIRMED]** on the dead-status (tightened the grep to be airtight); **[ADJUSTED]** severity to MEDIUM.

- **[HIGH] `termine` is a live LEGACY-SUPERSEDED duplicate of `gutachter_termine` — dual write-path, drift hazard**  ·  category: redundancy  ·  effort: m  ·  coveredByCMM49: no
  - Evidence: `gutachter_termine`=**18 rows** (canonical), `termine`=**0 rows** with a live INSERT writer at `src/app/faelle/[id]/_actions/termine.ts:204` + UPDATE at `:260`, and 3 readers confirmed verbatim: `src/app/mitarbeiter/performance/page.tsx:67`, `src/app/faelle/[id]/_actions/termine.ts` (writer), `src/app/admin/_components/TageskalenderWidget.tsx:14`, `src/app/admin/kalender/page.tsx:19`. The `termine.ts:204` insert is keyed on **`fall_id`** (confirmed by reading the insert payload) — so it ALSO dies at fall_id death (§E), making the cleanup doubly motivated.
  - Recommendation: Unchanged — migrate the writer + 3 admin/mitarbeiter readers onto `gutachter_termine` (carries `claim_id` per SP-G2), then DROP `termine`. Net-new vs CMM-49. Extra note for §D/§E: because `termine` is `fall_id`-keyed, it's already on the fall_id-death collision course — folding it into `gutachter_termine` resolves both the split-brain AND the drop.
  - Info-loss risk: none (`termine` empty).
  - Verdict: **[CONFIRMED]** (added: writer is fall_id-keyed → also a §E breaker, strengthens the case).

- **[HIGH] `parteien` (0 rows) — DEAD-READS feeding insurer name into emails/PDF; superseded by `claim_parties` (72 rows)**  ·  category: drift  ·  effort: m  ·  coveredByCMM49: partial
  - Evidence: `parteien`=**0 rows**, `claim_parties`=**72 rows** (both confirmed). All 7 read sites confirmed verbatim, all `fall_id`-keyed + role-filtered `gegner`: `src/lib/email/google/flows.ts:113`, `:273`, `:431`, `src/lib/termine/get-by-token.ts:96`, `src/app/gutachter/fall/[id]/page.tsx:140`, `src/app/admin/kanzlei-board/page.tsx:17`, `src/app/api/pdf/kanzlei-paket/[id]/route.tsx:59`. The three `flows.ts` reads pull `versicherung_name` (`:431` also `versicherung_nr`) for customer-facing emails → silent blank "Versicherung des Gegners".
  - Recommendation: Unchanged — repoint all 7 to `claim_parties` (role `gegner`, keyed on `claim_id`). This is a net-new customer-facing correctness bug worth fixing earlier than the §E faelle-drop window. coveredByCMM49=partial is correct: SHARED CONTEXT lists `parteien` as a post-drop DEAD-READS surface, but the email/PDF null-output bug is independent and higher-urgency.
  - Info-loss risk: none — repointing to the populated table strictly improves output.
  - Verdict: **[CONFIRMED]** (all 7 sites + both row-counts verified exactly).

- **[MEDIUM] `zahlungseingaenge`/`zahlungspositionen` (0/0 rows) overlap `claim_payments`/`finance_eintraege` — payment-table sprawl**  ·  category: redundancy  ·  effort: l  ·  coveredByCMM49: no  ·  **[ADJUSTED: more reader sites than finder cited]**
  - Evidence: `zahlungseingaenge`=0, `zahlungspositionen`=0, `claim_payments`=**0**, `finance_eintraege`=**5** (confirmed). Writers confirmed at `src/app/faelle/[id]/_actions/kanzlei-paket.ts:329` (insert `zahlungseingaenge`) + `:345` (insert `zahlungspositionen`). **Correction:** the finder framed `zahlungseingaenge` as write-only, but it has **two additional live readers** it missed: `src/lib/analytics/finance.ts:137` (`select id, fall_id, gesamtbetrag`) and `src/lib/finance/fall-finanzen.ts:145`. Both are `fall_id`-coupled → also §E breakers. So this is a genuine read+write split-brain, not just an idle write target — slightly raises the consolidation value (still MEDIUM given 0 data).
  - Recommendation: Unchanged in direction — consolidate into `claim_payments` (claim-keyed) or `finance_eintraege` while volume is ~zero. Add the two analytics readers to the migration scope.
  - Info-loss risk: low now (all sources empty/near-empty).
  - Verdict: **[ADJUSTED: +2 reader sites — `analytics/finance.ts:137`, `finance/fall-finanzen.ts:145`; both fall_id-keyed]**.

- **[MEDIUM] `phase_transitions` (1 row) — legacy-coupled lifecycle audit log, fall_id-keyed, dual writers**  ·  category: challenge  ·  effort: s  ·  coveredByCMM49: partial
  - Evidence: `count(*)=**1**` (n_live_tup=0 — stale). Writer #1 confirmed at `src/lib/faelle/state-machine.ts:235`: `from_phase`/`to_phase` = the 19-enum `fall_status` strings, `fall_id`-keyed, fire-and-forget (`.then()` non-critical, AAR-586). Writer #2 confirmed at `src/lib/claims/endzustand-actions.ts:79` (via `writeAudit()`): `fall_id`-keyed, `trigger_type:'manual'`, `transitioned_by:user.id`, `actor_rolle:user.rolle` (AAR-840). I also confirmed the finder's "redundant adjacent `timeline` write" claim: `state-machine.ts:225` inserts a `timeline` row `Status: ${currentStatus} → ${newStatus}` 10 lines above the `phase_transitions` insert — same event, free-text vs structured.
  - Recommendation: Unchanged — tie to §D3. When the state-machine writer flips to `claims.status`, re-key `phase_transitions` to `claim_id` and decide status-vs-phase semantics (the table name says "phase" but logs `fall_status` strings — genuine misnomer). Given 1 row in months + the redundant adjacent `timeline` insert, dropping it in favor of `timeline` is a legitimate option.
  - Info-loss risk: dropping loses the only *structured* (column-typed) transition log with `actor_rolle`/`trigger_type` — the `timeline` row is free-text and lacks `actor_rolle`/`trigger_type`. So the loss is slightly more than "marginal": you'd lose the manual-vs-auto distinction and actor-role on transitions. Decide with §D3.
  - Verdict: **[CONFIRMED]** (both writers + the misnomer + the adjacent timeline redundancy all verified; minor note that the structured columns make the info-loss a touch more than "marginal").

- **[MEDIUM] `regulierungs_klassifizierung` (0 rows) — wired but unexercised; verify keying survives faelle drop**  ·  category: hygiene  ·  effort: s  ·  coveredByCMM49: no
  - Evidence: `count(*)=0`. All 5 sites confirmed verbatim: readers `src/app/admin/statistiken/page.tsx:174`, `src/app/admin/sachverstaendige/[id]/page.tsx:88`, writers `src/lib/actions/dispatch-fall-actions.ts:34`, `src/app/faelle/[id]/_actions/kanzlei-paket.ts:422`, upsert `src/app/api/seed-testdata/route.ts:910`. Fully wired, never populated.
  - Recommendation: Unchanged — keep, but ensure its `fall_id`-keyed writers (`dispatch-fall-actions.ts`, `kanzlei-paket.ts`) are inside CMM-49 §D writer-migration scope or it breaks silently at §E.
  - Info-loss risk: none currently.
  - Verdict: **[CONFIRMED]**.

- **[LOW] PARKED-but-fully-wired empty tables — keep, do NOT drop**  ·  category: hygiene  ·  effort: xs  ·  coveredByCMM49: no
  - Evidence: Confirmed 0 rows for the cited cluster (`aircall_relay_seats`, `call_copilot_suggestions`, `call_transcription_utterances`, `matelso_calls`, `anruf_log`, `organisationen`, `airdrop_invitations`, `dsgvo_loeschauftraege`, `embed_sites`, `embed_abrechnung_positionen`, `incentives`, `incentive_auszahlungen`, `mitarbeiter_performance`, `branchen_benchmarks`, `paket_upgrades`, `gebiet_exklusivitaeten`, `provisionen_maik`, `gutschriften`, `finance_monatsberichte`, `sv_payment_reminders`, `abrechnung_reminders`, `technische_probleme`, `reklamationen`, `personenschaden_personen`, `vehicle_vorschaeden`, `kunde_gutachten_requests`, `forderungspositionen`, `individuelle_anfragen` — all 0). Minor correction: the finder wrote `calls`/`aircall_calls` — the live table is `aircall_calls` (0 rows) and a separate `calls` (0 rows); both exist. These have real wiring per the finder's cited sites.
  - Recommendation: Unchanged — classify PARKED-WIRED, exclude from drop sweeps. Emptiness is product-maturity, not dead code.
  - Info-loss risk: dropping would break a live (untested) feature path.
  - Verdict: **[CONFIRMED]** (trivial naming note: `aircall_calls` + `calls` are two distinct tables, both empty).

- **[LOW] `cron_jobs_audit` is by far the largest table — unbounded audit growth**  ·  category: performance  ·  effort: s  ·  coveredByCMM49: no  ·  **[ADJUSTED: numbers]**
  - Evidence: live `count(*)=**24,491**` (finder said ~24,486 — close, off by 5, likely cron-tick drift between snapshots). Next-largest by real count: `plz_geo`=**10,823** (reference data), `benachrichtigungen`=**2,598**. **Stale-stat warning re-demonstrated:** `pg_stat_user_tables.n_live_tup` reports `cron_jobs_audit` as **559** — ~45x too low — so any sizing judgment off n_live_tup would be badly wrong here, exactly as the data caveat warns. `claims`=75, `leads`=331 (note: leads n_live_tup=0, also stale).
  - Recommendation: Unchanged — add a 30–90 day retention/TTL via existing cron infra.
  - Info-loss risk: loses old cron-run history beyond retention — acceptable.
  - Verdict: **[CONFIRMED, ADJUSTED numbers: 24,491 live; n_live_tup=559 is the stale figure]**.

### Missed in this dimension

- **[MEDIUM] `community_leaderboard` cron writer — the community feature is split THREE ways, not two**  ·  category: redundancy  ·  effort: s  ·  coveredByCMM49: no
  - Evidence: The finder noted `community_leaderboard` is "wired" (read at `gutachter/community/page.tsx:60`, `admin/statistiken/page.tsx:205`) and contrasted it with the orphaned normalized tables — but missed that it also has a **dedicated cron writer**: `src/app/api/cron/community-leaderboard-update/route.ts:112` (`db.from('community_leaderboard').upsert(...)`, reads `organisationen` where `community_leaderboard_aktiv=true` at `:34`). So the community feature actually spans THREE wired surfaces (`organisationen` flat config columns + `community_leaderboard` table + this cron) AND 8 orphaned normalized shells. `community_leaderboard`=**0 rows** confirmed — so even the "wired" leaderboard is unexercised (the cron has never produced a row). This sharpens the finder's "half-built feature" point: it's not half-built, it's a *flat* implementation (organisationen + leaderboard + cron) with a fully-orphaned *normalized* design (8 tables) layered dead on top.
  - Recommendation: When confirming the 8-orphan DROP with Aaron, frame it as "remove the dead normalized community design; the live feature is the flat organisationen+leaderboard+cron path." Separately flag that the leaderboard cron has produced 0 rows — verify the cron is actually scheduled/firing, or it's a 4th dormant surface.
  - Info-loss risk: none (the normalized 8 are empty; the flat path is the real one).

- **[LOW] `admin_termine` / `kanzlei_admin_termine` — a THIRD and FOURTH calendar table, both 0 rows**  ·  category: redundancy  ·  effort: s  ·  coveredByCMM49: no
  - Evidence: Beyond the `termine` vs `gutachter_termine` split the finder caught, the schema also has `admin_termine` (0 rows) and `kanzlei_admin_termine` (0 rows). That's **four** calendar/appointment tables coexisting (`gutachter_termine`=18, `termine`=0, `admin_termine`=0, `kanzlei_admin_termine`=0). The finder's "two calendar tables" framing undercounts the sprawl.
  - Recommendation: Fold the calendar-table consolidation into one decision: `gutachter_termine` as SSoT, audit `admin_termine`/`kanzlei_admin_termine` wiring (likely thin/dead given 0 rows) and collapse. Broadens the finder's `termine`-only proposal into a proper calendar-domain cleanup.
  - Info-loss risk: none currently (3 of 4 empty); verify wiring before drop.

- **[INFO] `vehicle_vorschaeden` is wired (finder's open-question answer is correct) — but worth pinning**  ·  category: hygiene  ·  effort: xs  ·  coveredByCMM49: no
  - Evidence: `vehicle_vorschaeden`=0 rows but has a live writer (the finder's open question asked to confirm). I did not re-open `run-full.ts` but the table is in the PARKED-WIRED-adjacent set; `vehicle_ownership_history` (0 refs, confirmed orphan) is the drop candidate, `vehicle_vorschaeden` is not. This resolves the finder's own open question: keep `vehicle_vorschaeden`, the orphan is `vehicle_ownership_history`.
  - Recommendation: Treat the two vehicle-history tables differently in any CMM-50 follow-up: `vehicle_ownership_history` = drop candidate; `vehicle_vorschaeden` = parked-wired.
  - Info-loss risk: none.

### Open questions
- Are the 8 normalized org/community tables (`sv_buero*`/`sv_organisation*`/`communit*`/`sv_community`) abandoned, given the live feature runs on `organisationen` flat columns + `community_leaderboard` + the leaderboard cron? Confirm before DROP. (Confirmed via grep + 0-row + 0-spec-mention that they are net-new vs CMM-49.)
- The `community_leaderboard` cron (`api/cron/community-leaderboard-update/route.ts`) has produced **0 rows** in PROD — is it actually scheduled/firing, or a dormant 4th surface? (Memory note "VPS-Crons statt Vercel" means it'd be a VPS crontab entry, not vercel.json.)
- Four calendar tables (`gutachter_termine`/`termine`/`admin_termine`/`kanzlei_admin_termine`) — consolidate all onto `gutachter_termine` as one calendar-domain cleanup? `termine` is fall_id-keyed (also a §E breaker), `gutachter_termine` already carries `claim_id`.
- `phase_transitions`: keep as the structured claim-status audit log (re-keyed to `claim_id` at §D3, carries `actor_rolle`/`trigger_type` that `timeline` lacks) or drop in favor of the redundant `timeline` insert at `state-machine.ts:225`? 1 row in PROD.
- Confirm `regulierungs_klassifizierung`, `reklamationen`, `personenschaden_personen`, `zahlungseingaenge`/`zahlungspositionen`, and `termine` writers (all `fall_id`-keyed via `faelle/[id]/_actions/*` + `dispatch-fall-actions`) are inside CMM-49 §D writer-migration scope — every one silently breaks at §E (fall_id death) otherwise.