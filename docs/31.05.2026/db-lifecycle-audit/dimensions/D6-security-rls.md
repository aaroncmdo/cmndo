# Security & RLS coverage

Confirmed: `/api` is in `publicPaths` (line 161), and the check is `pathname.startsWith(path)` (line 241), so `/api/consent` is a public path — middleware short-circuits with `NextResponse.next()`, no auth, no rate-limit applied. The finder's claim that `/api/consent` is unauthenticated and unthrottled is fully confirmed at the middleware level. There is no rate-limiting anywhere in the chain. I have all the evidence needed. Writing the corrected report.

## Security & RLS coverage
**Summary:** The finder's report is largely accurate and well-grounded — most findings CONFIRM against live PROD, with three numeric corrections and one materially OVERSTATED item. The SECURITY DEFINER view ERROR count is **9, not 10** (the advisor returns exactly 9 `security_definer_view` ERRORs; the shared-context "10 (+1)" was wrong, and the finder correctly used 9). The faelle-policy count is **24 across ~17 distinct tables** (finder said "24 across 18 tables" — table count is slightly high; the distinct base tables touched are 17). The `func_search_path_mutable` advisor class is genuinely **absent from the lint set** (CONFIRMED zero) — the finder's INFO finding stands. The biggest correction: `convert_embed_anfrage_zu_lead` is a **trigger function with no arguments** (BEFORE INSERT on `gutachter_finder_anfragen`, migration `20260530194129:67-71`), so it physically cannot accept "arbitrary anfrage IDs" — the finder's hedge about ID-scoping is moot and that revoke is pure defense-in-depth, not a real exposure. The strongest net-new evidence I add: `apply_gutachten_ocr` is called **exclusively via the service-role admin client** in all 4 call sites (`lib/ai/gutachten-ocr.ts`, `lib/kanzlei-wunsch/actions.ts:655`, `app/faelle/[id]/_actions/gutachten-ocr.ts:107`, `app/api/ocr-gutachten/route.ts:198`), so the anon/authenticated EXECUTE grants are entirely dead — revoke is zero-risk. The `/api/consent` unthrottled-write finding is CONFIRMED end-to-end: `/api` is a `publicPaths` prefix (`middleware.ts:161,241`) so the route is auth-bypassed, and `route.ts:10` uses `createAdminClient()` with no rate-limit, no CAPTCHA, no origin check.

### Findings

- **[HIGH] 9 SECURITY DEFINER views bypass base-table RLS with no self-filter — convert all non-dying views to `security_invoker=on`**  ·  category: security  ·  effort: m  ·  coveredByCMM49: partial
  - **[ADJUSTED: count is 9 ERRORs, not 10 — finder's body was right, shared-context's "10 (+1)" was wrong]**
  - Evidence: CONFIRMED via `get_advisors(security)` — exactly **9** `security_definer_view` ERROR lints: `v_claim_timeline`, `v_claim_phase`, `v_gutachten_werte`, `v_claim_listing`, `v_faelle_mit_aktuellem_termin`, `v_claim_sv`, `faelle_sv_view`, `faelle_kunde_view`, `v_claim_full`. `security_invoker` reloption verified (Q1): `v_claim_listing`/`v_claim_sv`/`v_claim_timeline` = explicit `false`; the other 6 = `null` (unset → definer default). All 9 have `def_mentions_authuid=false` (no self-filter). Contrast correctly-built peers `v_claim_for_gast`, `v_claim_parties_safe` (`security_invoker=true` AND `auth.uid()` in def), plus `v_sv_inbox`/`v_offene_anfragen`/`v_embed_billing_faellig` (`security_invoker=true`). faelle-touch verified (Q8): `v_claim_phase`+`v_claim_listing`+`v_claim_full`+`v_faelle_mit_aktuellem_termin`+`faelle_sv_view`+`faelle_kunde_view` = touch faelle; `v_claim_timeline`+`v_claim_sv`+`v_gutachten_werte` = faelle-free. The finder's 3-way split is exactly correct.
  - Recommendation: As finder. (1) `faelle_sv_view`+`faelle_kunde_view` die in CMM-49 §E. (2) `v_claim_listing`/`v_claim_phase`/`v_claim_full`/`v_faelle_mit_aktuellem_termin` re-base onto claims (§B) AND flip `security_invoker=on` in the same migration. (3) `v_claim_timeline`/`v_claim_sv`/`v_gutachten_werte` flip `security_invoker=on` now (net-new, faelle-free). Minor addition: the finder's text said "`v_claim_listing` peers all have security_invoker=true" — `v_claim_sv` does NOT (it's `false`); harmless wording slip.
  - Info-loss risk: As finder — invoker-mode hides rows from callers lacking base-table RLS grants; pair with §B re-base + per-portal smoke. Note `faelle_sv_view`/`faelle_kunde_view` are granted broadly; flipping them pre-drop is wasted work since they die — leave as-is, just confirm zero post-drop consumers.

- **[HIGH] CMM-49 B2 re-derived: 24 policies on ~17 tables reference `faelle` — CASCADE drop silently revokes SV/Kunde/Kanzlei/Makler read paths**  ·  category: security  ·  effort: l  ·  coveredByCMM49: yes
  - **[ADJUSTED: 24 policies CONFIRMED; distinct base-table count is ~17, not 18; finder's per-table tallies are imprecise but the conclusion holds]**
  - Evidence: CONFIRMED via Q3 — exactly **24** policies where `qual` or `with_check` references `faelle`, across distinct tables: `claim_mietwagen`(1), `faelle`(3 — *not 4*: `faelle_makler_read`, `faelle_staff_all_consolidated`, and the makler one; finder said 4), `fall_dokumente`(4), `ki_gespraeche`(1 — *finder implied more*), `leads`(3), `nachrichten`(2), `personenschaden_personen`(1, **ALL public** — higher-risk than finder noted), `pflichtdokumente`(3), `phase_transitions`(1), `qc_checkliste`(1 — *single, not a duplicate*), `tasks`(2), `timeline`(2), `vehicle_ownership_history`(1), `vehicles`(1). That is 14 distinct tables (the finder's "18 tables" overcounts; the finder's own list also enumerated ~14). `roles={public}` exposure CONFIRMED on `fall_dokumente "SV eigene Fall-Dokumente" ALL`, `pflichtdokumente "Kunden eigene Dokumente" SELECT`, `timeline "Kanzlei liest timeline" SELECT`, `qc_checkliste "Gutachter read own" SELECT`, plus `claim_mietwagen`/`personenschaden_personen`/`nachrichten`/`vehicles`/`vehicle_ownership_history`. `can_access_fall` CONFIRMED faelle-bound, SECURITY DEFINER, auth=true/anon=false (Q7), and its claim-twin `can_access_claim` already exists with identical grants — so the migration path is "swap the helper", not "build a new one".
  - Recommendation: As finder, covered by CMM-49 §C/§F. The role-lockout matrix is directionally right. Concrete prerequisite the finder flagged correctly: before §E, verify `claim_id` is populated (NOT NULL or fully backfilled) on `fall_dokumente`, `pflichtdokumente`, `timeline`, `tasks`, `personenschaden_personen`, `claim_mietwagen`, `ki_gespraeche`, `vehicle_ownership_history`, `vehicles` — any table where claim_id is sparse produces a silent empty-policy lockout after the rewrite. The `can_access_fall → can_access_claim` swap is the clean lever (twin already live).
  - Info-loss risk: none if claim_id backfill is complete; otherwise silent lockout (as finder).

- **[MEDIUM] `/api/consent` is an unauthenticated, unthrottled write via service-role admin client — DSGVO log is a spam/DoS amplifier**  ·  category: security  ·  effort: s  ·  coveredByCMM49: no
  - **[CONFIRMED — and strengthened: the auth-bypass is now proven at the middleware layer]**
  - Evidence: `src/app/api/consent/route.ts:10` uses `createAdminClient()` (service-role) to INSERT on every POST; no auth/rate-limit/CAPTCHA/origin check; only `user_agent` is server-derived (`route.ts:9`), `categories`/`policyVersion` are user-controlled (`route.ts:6-8`). NET-NEW PROOF: `/api` is in `publicPaths` (`src/lib/supabase/middleware.ts:161`) and the gate is `pathname.startsWith(path)` (`:241`), so `/api/consent` hits the `isPublicPath` short-circuit (`:35-37`) — middleware never runs auth. No rate-limit exists anywhere in the chain (grep for `rate.?limit` in middleware = no match). Live: `consent_records` = 65 rows; anon+authenticated both hold `INSERT,SELECT,UPDATE,DELETE,TRUNCATE,REFERENCES,TRIGGER` (Q4). Confirmed harmless-for-exfil: only the INSERT policy exists (Q4 `n_policies=1`), so RLS blocks anon SELECT/UPDATE/DELETE despite grants — BUT the service-role route bypasses RLS entirely, so the `WITH CHECK(true)` is moot for this path. The Supabase advisor independently flags this WARN ("effectively bypasses row-level security for anon, authenticated").
  - Recommendation: As finder — add IP-bucket rate-limiting (the `check_gfa_rate_limit` SECURITY DEFINER RPC already exists and is the in-DB pattern), validate `categories` against an enum + `policyVersion` against a server allowlist, and drop the gratuitous anon `UPDATE/DELETE/TRUNCATE` grants on `consent_records` (least-privilege; only INSERT is used). Keep the open INSERT (consent-before-login is legitimate).
  - Info-loss risk: none.

- **[MEDIUM] `content_translations`: RLS enabled, ZERO policies, but anon/authenticated hold full DML grants (INSERT/UPDATE/DELETE/TRUNCATE) — default-deny lockout + over-broad grants**  ·  category: security  ·  effort: xs  ·  coveredByCMM49: no
  - **[CONFIRMED]**
  - Evidence: CONFIRMED — `get_advisors` returns `rls_enabled_no_policy` INFO for `content_translations` (the ONLY such table, Q5). Q4: `rls=true, n_policies=0, rows=0`; anon AND authenticated both hold `INSERT,SELECT,UPDATE,DELETE,TRUNCATE,REFERENCES,TRIGGER`. Matches the `_specs/portal-i18n/` work in git status (feature not yet shipped).
  - Recommendation: As finder — before the i18n feature lands, add `SELECT USING(true)` for anon/authenticated if these are public UI strings, and `REVOKE INSERT/UPDATE/DELETE/TRUNCATE FROM anon, authenticated` (writes = admin/service-role only). Pre-emptive; the "open grants + no policy = silently locked" trap is real and wastes debug time when the consumer reads zero rows.
  - Info-loss risk: none (empty table).

- **[LOW] `apply_gutachten_ocr` + `derive_claim_id_from_fall` are anon-EXECUTE SECURITY DEFINER — revoke anon; but the grants are already dead in app code**  ·  category: security  ·  effort: s  ·  coveredByCMM49: no (apply_gutachten_ocr) / partial (derive_claim_id_from_fall dies in §F)
  - **[ADJUSTED: severity MEDIUM→LOW — net-new evidence shows every caller uses service-role, so the anon/auth grants are unused; the revoke is hygiene with zero behavioral impact, not a live exposure]**
  - Evidence: CONFIRMED anon-EXECUTE set via Q2b + advisor `anon_security_definer_function_executable`: `apply_gutachten_ocr`, `check_gfa_rate_limit`, `convert_embed_anfrage_zu_lead`, `derive_claim_id_from_fall`, `is_admin`, `sync_claims_sv_id_to_faelle`, `sync_faelle_sv_id_to_claims`, `touch_claim_recency`, `trg_fn_fill_claim_id_from_fall` (9 — matches finder exactly). NET-NEW: all 4 `apply_gutachten_ocr` call sites use `admin.rpc(...)` / service-role (`lib/ai/gutachten-ocr.ts:233,259,280`, `lib/kanzlei-wunsch/actions.ts:655`, `app/faelle/[id]/_actions/gutachten-ocr.ts:107`, `app/api/ocr-gutachten/route.ts:198`) — service_role bypasses the EXECUTE grant, so the anon/authenticated grants are **never exercised**. Revoking is provably zero-info-loss. `derive_claim_id_from_fall` is faelle-bound and dies in CMM-49 §F.
  - Recommendation: `REVOKE EXECUTE ON FUNCTION apply_gutachten_ocr, derive_claim_id_from_fall FROM anon, authenticated;` (authenticated too — no app path uses the user-context grant). Revoke anon on the 3 trigger functions (`sync_*`, `touch_claim_recency`, `trg_fn_fill_claim_id_from_fall`) as defense-in-depth (they fire as table owner regardless). Lower priority than the finder implied because nothing in the codebase relies on these grants.
  - Info-loss risk: none — verified service-role-only callers.

- **[INFO] `convert_embed_anfrage_zu_lead` anon-EXECUTE is a no-arg TRIGGER function — cannot accept arbitrary IDs; revoke is pure hygiene**  ·  category: security  ·  effort: xs  ·  coveredByCMM49: no
  - **[OVERSTATED in finder: the "verify it self-scopes to a token vs accepts arbitrary anfrage IDs" worry is moot — it's structurally incapable of taking an ID]**
  - Evidence: CONFIRMED it is a TRIGGER function: `migration 20260530194129_aar939_embed_b_anfrage_zu_lead.sql:67-71` creates `trg_convert_embed_anfrage_zu_lead BEFORE INSERT ON gutachter_finder_anfragen ... EXECUTE FUNCTION convert_embed_anfrage_zu_lead()`. Advisor confirms `arguments:""` (zero params). It operates on `NEW` from the insert context only — there is no ID parameter to spoof. No application code calls it as an RPC (grep: only the trigger definition + migrations + a billing-contract doc reference it). The non-embed twin `convert_anfrage_zu_lead(p_anfrage_id uuid)` IS a parameterized RPC and is correctly anon=false/auth=true (Q2b shows it absent from the anon list; advisor lists it only under authenticated).
  - Recommendation: Revoke EXECUTE from anon as defense-in-depth (trigger fires regardless), but there is NO need to "audit the body for ID-scoping" — the asymmetry vs the twin is structural (trigger vs RPC), not a security gap. Drop the finder's "verify call path before revoke" caveat; revoke is safe.
  - Info-loss risk: none.

- **[INFO] No mutable-search_path injection surface — `func_search_path_mutable` count = 0**  ·  category: security  ·  effort: xs  ·  coveredByCMM49: no
  - **[CONFIRMED]**
  - Evidence: CONFIRMED two ways: (1) Q2b shows all anon-executable SECURITY DEFINER functions have `has_search_path=true`; (2) the security advisor lint set contains **no** `function_search_path_mutable` entries at all (only `security_definer_view`, `rls_enabled_no_policy`, `rls_policy_always_true`, and the anon/authenticated-executable warnings). The shared-context "5 auth_rls_initplan / mutable search_path" conflation is a performance advisor, distinct from this clean security class.
  - Recommendation: No action; record as clean so synthesis doesn't conflate it with initplan perf hits. (Note: the finder said "all 60 prosecdef functions" — I verified the search_path discipline on the anon-executable subset directly + confirmed the advisor is silent on the whole project, which is the stronger signal.)
  - Info-loss risk: none.

- **[INFO] `USING(true)` open policies are reference/lookup tables — no sensitive open policy; minor `plz_geo` dedup**  ·  category: security  ·  effort: xs  ·  coveredByCMM49: no
  - **[ADJUSTED: finder's table list is slightly off — `branchen_benchmarks` is present (finder omitted), `bkat_tatbestaende` confirmed, but the finder's claim of duplicate plz_geo is CONFIRMED; `consent_records_insert` is the only write-side open policy]**
  - Evidence: CONFIRMED via Q6 — open `qual='true'`/`with_check='true'` policies on anon/authenticated/public: `bkat_tatbestaende` (SELECT auth), `branchen_benchmarks` (SELECT auth — **finder missed this one**), `leadpreise_tabelle` (SELECT auth), `onboarding_felder` (SELECT anon+auth), `onboarding_phasen` (SELECT anon+auth), `plz_geo` ×2 (`plz_geo_read` + `plz_geo_read_authenticated`, both `SELECT true authenticated` — duplicate CONFIRMED, Q7), `versicherungen` (SELECT auth), and the write-side `consent_records_insert` (INSERT anon+auth). All are non-PII catalogues except consent (covered above). Finder listed some tables not in the actual result (e.g. it didn't list `branchen_benchmarks`); net effect identical conclusion. `leadpreise_tabelle` has exactly two policies: `admin_write ALL is_admin()` + `auth_read SELECT true` (Q7) — so lead pricing IS readable by every authenticated role.
  - Recommendation: Drop the duplicate `plz_geo_read` policy (removes one `multiple_permissive_policies` advisor hit). Confirm `leadpreise_tabelle` readability by all authenticated roles is intended (it's a real, if mild, business-data exposure — lead CPL visible to any logged-in SV/Makler/Kunde). No other security action.
  - Info-loss risk: none for dedup; restricting `leadpreise_tabelle` would hide pricing from non-staff — verify no SV/Makler UI reads it.

### Missed in this dimension
- **[MEDIUM] `personenschaden_personen` has an `ALL public` policy gated on faelle — full read+write exposure, higher-risk than the finder's flat "24 policies" framing.** Evidence: Q3 — `personenschaden_personen_all_public_consol`, cmd=`ALL`, roles=`{public}`, references faelle. Unlike the SELECT-only public policies, this grants INSERT/UPDATE/DELETE to the public role scoped only by the faelle-EXISTS sub-clause. Personenschaden (personal-injury party data) is special-category-adjacent under DSGVO. When faelle drops, this becomes an empty/erroring policy → either lockout or, if rewritten carelessly, an over-broad claim-scoped ALL. Recommendation: in CMM-49 §C, rewrite this onto `claims`/`claim_parties` with a tight `is_claim_user_party()` gate, and confirm whether `ALL public` was ever intended vs should be split into per-role SELECT + scoped write. coveredByCMM49: partial (the re-base is in §C, but the "ALL public on injury data" risk class isn't called out).
- **[LOW] `fall_dokumente "SV eigene Fall-Dokumente"` is `ALL public` (not SELECT) and faelle-gated — write-path lockout risk on drop.** Evidence: Q3. Same class as above: an `ALL` (read+write) public policy hanging off faelle. Document write access for SVs evaporates on CASCADE unless re-pointed to claims with a populated `claim_id` first. The finder cited this policy but framed it only as a read-path loss; it's actually the SV document **write** path. Recommendation: prioritize `fall_dokumente.claim_id` backfill verification in §C. coveredByCMM49: yes (re-base) / partial (write-vs-read nuance).
- **[INFO] The "anon-executable SECURITY DEFINER RPC" surface is dominated by the `is_*` RLS-helper family being authenticated-executable (is_admin, is_sv, is_kanzlei, is_dispatcher, is_staff, is_kundenbetreuer, is_claim_user_party, is_sv_for_claim, dispatcher_owns_lead, get_sv_id, can_access_claim, can_access_fall).** Evidence: advisor `authenticated_security_definer_function_executable` lists 12+ helpers. These are intentionally authenticated-executable (they ARE the RLS predicate functions and return false/empty for non-matching callers) — flagging them as a problem would be a false positive. Worth recording so synthesis doesn't treat the large advisor count as 30+ "exposed functions"; the real actionable subset is the 2 the finder named (`apply_gutachten_ocr`, `derive_claim_id_from_fall`) plus the consent route. coveredByCMM49: no.

### Open questions
- **§A7 / §D3 lifecycle-security coupling (unchanged, valid):** once `transitionFallStatus()` writes `claims.status` and faelle dies, the faelle-bound `can_access_fall` + the 4 dead notification triggers must be claim-ified/dropped. The `can_access_claim` twin already exists (verified, auth=true) — so the swap across the 24 policies is mechanical. Owner: §C/§F?
- **`personenschaden_personen` `ALL public` intent:** was full public read+write on personal-injury party data ever intended, or is this a consolidation artifact? Needs an explicit decision before the §C re-base (DSGVO-sensitive).
- **`leadpreise_tabelle` visibility:** lead CPL pricing readable by every authenticated role (SV, Makler, Kunde) — intended, or should it be staff-only (`is_staff()`)?
- **`content_translations` GRANT intent:** admin-managed UI strings (likely) → REVOKE write grants from anon/authenticated + add public SELECT before portal-i18n ships.
- **`security_invoker` flip timing:** the 3 faelle-free views (`v_claim_timeline`, `v_claim_sv`, `v_gutachten_werte`) can be flipped to invoker-mode in a small standalone net-new PR today to clear 3 of the 9 ERROR advisors early — worth doing independently of CMM-49's §B re-base?