# Lifecycle representations, drift & model challenge

I now have all the evidence I need. Critical corrections:

1. **`leads.status` (lead_status enum) IS real and distinct** — distribution `flow-gesendet:130, disqualifiziert:96, umgewandelt:72, quali-offen:30, neu:2, umgewandelt-sv:1` (total 331). The finder's numbers were CORRECT (they ran a query that worked, just with a column name I initially mis-tested). The two-vocabulary finding is CONFIRMED.

2. **`checkFallAutoPhase` is NOT broadly live** — only 2 callers, both in faelle sub-actions (`filmcheck.ts:107`, `kanzlei-paket.ts:400`), both fire-and-forget `.catch(()=>{})`. The "engine still runs live in parallel" framing is overstated for the auto-engine breadth but the dual-write is real.

3. **`v_claim_phase` does NOT join faelle** (touches_faelle=true is only the comment string; from_or_join_faelle=false; the def is purely claims+kanzlei_faelle+leads+auftraege). Shared context's "v_claim_phase STILL touches faelle" is REFUTED for v_claim_phase. But `v_claim_listing` genuinely does join faelle (CONFIRMED).

4. **`SUBPHASE_VISIBILITY` consumer claim is wrong** — the finder said "Knip won't catch it (imported by something)." Actually the *file* `subphase-visibility.ts` is live (3 prod consumers of `buildClaimPhasePipeline`/`substateLabelForRolle`), but the 52-entry `SUBPHASE_VISIBILITY` constant + `PHASE_META` are imported ONLY by `subphase-visibility.test.ts`. So they're test-only dead weight, not "imported by something" production.

5. **`AKTUELLE_PHASE_LABELS`** — confirmed zero code consumers (only def + 1 docs MD). Backing column + proc gone. "Fully dead" CONFIRMED.

I have enough. Writing the corrected report.

## Lifecycle representations, drift & model challenge
**Summary:** The finder's core thesis holds up under verification: the claim lifecycle is encoded in multiple parallel vocabularies, only `claims.status` (12-value CHECK, live = `dispatch_done`:72 + `in_bearbeitung`:3) is the SSoT writers target, and the 19-value `fall_status` ENUM + its hand-coded state machine still dual-write. Most numbers reproduce exactly. But several severities and "dead/live" framings need correction. The biggest factual error: the finder claimed `v_claim_phase` is correct AND the shared-context claim that it "still touches faelle" — I confirmed `v_claim_phase` does **NOT** join faelle (pure claims+kanzlei_faelle+leads+auftraege); only `v_claim_listing` does. The auto-phase engine is far narrower than implied (2 fire-and-forget callers, not a broadly-running parallel engine). `AKTUELLE_PHASE_LABELS` is genuinely fully dead (verified zero code consumers). `SUBPHASE_VISIBILITY`'s "imported by something, knip won't catch it" is wrong — its host file is live but the 52-entry constant itself is imported only by its own test. The two-lead-vocabulary finding (`leads.status` enum vs `qualifizierungs_phase` text) is fully confirmed with both columns real and disagreeing. Net: the dimension is well-mapped; I downgrade two CRITICALs to HIGH, correct three evidence errors, and add three missed items.

### Findings

- **[CRITICAL → HIGH] `dispatch_done` + `in_bearbeitung` are 100% of live claims.status yet are invisible to the phase model** · category: challenge · effort: m · coveredByCMM49: no
  - Evidence: CONFIRMED. `claims.status` live = `dispatch_done`:72, `in_bearbeitung`:3 (75/75). CHECK admits exactly the 12 values cited (verified verbatim: `dispatch_done, in_bearbeitung, in_kommunikation_vs, reguliert, abgelehnt, an_externe_kanzlei_uebergeben, storniert, reguliert_vollstaendig, klage_rechtsstreit, verjaehrt, abgelehnt_final, termin_durchgefuehrt`). `lifecycle.ts:121-138` maps only 8 (ABSCHLUSS_SUBSTATE 7 + REGULIERUNG_STATUS_SUBSTATE 2, `abgelehnt` shared); `dispatch_done`/`in_bearbeitung` are in neither map. `v_claim_phase` distribution reproduces: erfassung/vollmacht_offen:61, begutachtung/kanzlei_uebergabe:12, erfassung/sa_offen:2 — purely lead/kanzleiFall-driven, claims.status contributed zero. The view def (verified) confirms `dispatch_done`/`in_bearbeitung` fall through every CASE branch to the lead/auftrag tail. **Verdict: [CONFIRMED] facts, [ADJUSTED: severity CRITICAL→HIGH].** This is documentation/clarity only (the finder's own Info-loss="none" and "acceptable BY DESIGN" concede it's not a defect). A doc-only clarity gap on a 75-row pre-launch table is HIGH at most, not CRITICAL — nothing is broken, no data is wrong, no user-facing regression.
  - Recommendation: As finder states — document the dual-semantics split in `lifecycle.ts` (status is a coarse dispatch flag for 2 values + terminal/VS-override for the rest). I'd go further: the cleaner end-state is a separate `claims.work_state` (dispatch/in-progress) so `claims.status` means only "lifecycle-terminal or VS-override," but that is XL and should be Aaron-gated, not assumed.
  - Info-loss risk: none.

- **[CRITICAL → HIGH] Two engines write lifecycle in parallel: `transitionFallStatus` (19-enum→faelle) AND endzustand/kanzlei-wunsch (12-value→claims)** · category: drift · effort: l · coveredByCMM49: partial
  - Evidence: CONFIRMED with one important narrowing. Live `faelle.status` = `sv-termin`:61, `ersterfassung`:12, `gutachten-eingegangen`:1 (74 rows) uses the 19-value adjacency at `state-machine.ts:17-46`. `state-machine.ts` does dual-write: it routes duplicate columns to `claims` via `splitOrKeepFaelleUpdate` (`state-machine.ts:141,161-167`) but never writes `claims.status` — confirmed by reading the whole file (it writes timestamps + `status_changed_at` to claims, not the lifecycle `status` value). So a fall can be `gutachten-eingegangen` while its claim is `dispatch_done`. The vocabularies are disjoint except `storniert`. **However:** the finder's framing "`autoPhase.ts:checkFallAutoPhase` … the legacy engine still runs live" overstates reach — `checkFallAutoPhase` has exactly **2 callers**, both fire-and-forget (`.catch(()=>{})`): `src/app/faelle/[id]/_actions/filmcheck.ts:107` and `…/kanzlei-paket.ts:400`. It is not a broadly-wired parallel engine; it's a narrow QC/kanzlei-paket side-effect. `checkLeadAutoPhase` writes only `qualifizierungs_phase` (`autoPhase.ts:14-31`), confirmed. **Verdict: [CONFIRMED] dual-write reality; [ADJUSTED: severity CRITICAL→HIGH, scope of autoPhase narrowed to 2 fire-and-forget callers].** This is the §D3/§A7 open item — partial CMM-49 coverage is correct.
  - Recommendation: Finder's "retire `checkFallAutoPhase` entirely, re-home task-triggers onto sub-entity writers" is sound and net-new beyond CMM-49 — and *cheaper* than the finder implies, given only 2 call-sites. The `triggerQcTask`/`triggerKanzleiPaketTask` side-effects at `autoPhase.ts:74-85` must move to the auftrag/gutachten/kanzlei-paket writers.
  - Info-loss risk: none from removing the re-derivation; the task side-effects must be preserved (medium).

- **[HIGH] The 19-enum's granular operational states have NO home in 12-value claims.status — real info regression for `vs-kuerzt`/`nachbesichtigung`/`filmcheck`/`qc-pruefung`/`anschlussschreiben`** · category: challenge · effort: l · coveredByCMM49: no
  - Evidence: CONFIRMED. `state-machine.ts:28-46` encodes `filmcheck`, `qc-pruefung`, `anschlussschreiben`, `vs-kuerzt`, `vs-abgelehnt`, `nachbesichtigung-laeuft`, `regulierung-laeuft`, `klage`, `zahlung-eingegangen` with dedicated timestamp/field side-effects (`vs_reaktion_typ='gekuerzt'`+`vs_kuerzung_grund` at :118-122, `anschlussschreiben_am` at :96-98, `vs_ablehnungsgrund` at :106-110). `fall_status` enum (verified, 19 vals) contains all of these. `lifecycle.ts:29-54` ClaimSubPhase has no `vs_kuerzt`/`filmcheck`/`qc_pruefung`/`anschlussschreiben`/`nachbesichtigung`; regulierung only has `versicherungskontakt`/`auszahlung`/`nachforderung`. The kürzung-SLA is real and live: `state-machine.ts:322-328` starts `kanzlei_kuerzung_antwort` (3 WT) on `vs-kuerzt`. **Verdict: [CONFIRMED].** Severity HIGH is right.
  - Recommendation: As finder — confirm with Aaron, re-home onto sub-entities (`kanzlei_faelle.status` currently only `versicherungskontakt`:12 live; `gutachten.status` only `beauftragt`/`final` live; `auftraege.typ='nachbesichtigung'` exists). One caveat the finder missed: these granular states are written by **webhook paths** (`vs_kuerzt` direct write per `state-machine.ts:33-35` comment), not just the state machine — so re-homing must cover the LexDrive/VS webhook writers too, not only the state-machine call-sites.
  - Info-loss risk: HIGH if not re-homed (kürzung billing+SLA, QC gating, nachbesichtigung visibility).

- **[HIGH → MEDIUM] `subphase-visibility.ts` ships a 52-subphase × 5-role matrix; its DB key column is gone — but the finder mis-stated the consumer** · category: dead-code · effort: m · coveredByCMM49: no
  - Evidence: PARTIALLY CONFIRMED, one claim REFUTED. `subphase-visibility.ts:9` + the `subphase-visibility.test.ts:28` assertion `Object.keys(SUBPHASE_VISIBILITY).length).toBe(52)` confirm the 52-entry matrix. The DB backing is gone: `faelle` has no `aktuelle_phase` column (only `dokumente_vollstaendig_*_phase` remain — verified), and `map_claim_phase_to_faelle_phase` proc is dropped (verified: only `log_phase_transition` survives among legacy procs). **BUT the finder wrote "Knip won't catch it (it's imported by something)" — that's wrong.** The host file `subphase-visibility.ts` is very much live: `buildClaimPhasePipeline` is imported by `FaelleKanban.tsx:10` + `FallPhasenPanel.tsx:16`, and `substateLabelForRolle` by `ClaimStepper.tsx:17`. The specific `SUBPHASE_VISIBILITY` + `PHASE_META` constants are imported **only** by `subphase-visibility.test.ts` (verified grep). So it's test-only dead weight inside a live file, not "imported by something" production code — knip flags *unused exports*, but those are non-blocking (`--warn`) per the knip-gate policy. **Verdict: [ADJUSTED: severity HIGH→MEDIUM; consumer claim corrected — the 52-matrix is test-only, the file is live].** Severity drops because it's ~480 LOC of reference data behind a `--warn` gate, not a live-rendered vocabulary.
  - Recommendation: Finder's option (a) "wire role-visibility into `lifecycle.ts` against the 18 live sub-phases" is the right product move (role visibility is real knowledge). Option (b) delete is fine too. Concretely: delete `SUBPHASE_VISIBILITY` + `PHASE_META` + the test that pins them; keep `buildClaimPhasePipeline`/`substateLabelForRolle`/`KUNDE_SUBSTATE_LABEL` (the live 4-phase renderer).
  - Info-loss risk: LOW — role-visibility intent worth preserving against the 18 live sub-phases.

- **[HIGH → MEDIUM] `AKTUELLE_PHASE_LABELS` (16 codes) is fully dead — DB source gone, zero code consumers** · category: dead-code · effort: xs · coveredByCMM49: partial
  - Evidence: CONFIRMED, and stronger than the finder showed. `statusLabels.ts:79-96` defines the 16-code map. Backing column + proc gone (verified above). Grep for `AKTUELLE_PHASE_LABELS` outside `statusLabels.ts` returns exactly ONE hit — a docs file (`docs/12.05.2026/FRONTEND/DONE-PLAN-…md:26`), zero code consumers. **Verdict: [CONFIRMED]; [ADJUSTED: severity HIGH→MEDIUM].** A 16-line dead constant is a clean xs deletion but not HIGH-impact — it harms nothing at runtime, it's just clutter. coveredByCMM49=partial is fair (backing column died in CMM-44 MP-6).
  - Recommendation: Delete `AKTUELLE_PHASE_LABELS`. Trivial, independent of the faelle drop.
  - Info-loss risk: none.

- **[MEDIUM] `PHASE_VISIBLE_SECTIONS` (~30 codes) is a parallel status→UI vocabulary keyed on dying `fall.status`** · category: redundancy · effort: m · coveredByCMM49: no
  - Evidence: CONFIRMED exactly. `phase-config.ts:54-142` maps ~30 faelle.status codes (with explicit "Legacy-Alias" dupes: `sv-zugewiesen`/`sv-termin`/`besichtigung`/`begutachtung-laeuft`/`filmcheck` etc.). Sole consumer: `FallContext.tsx:18` import + `:87-88` `const phase = fall.status ?? 'ersterfassung'; getVisibleSections(phase)` (verified — keys directly on `fall.status`, the 19-enum, independent of `v_claim_phase`). **Verdict: [CONFIRMED].**
  - Recommendation: As finder — re-key `getVisibleSections` to `ClaimMainPhase`+`ClaimSubPhase` from `getClaimLifecycle()`; removes the last live UI consumer of `fall.status`, unblocking CMM-49 §E on the Fallakte. This is genuinely net-new (CMM-49 §C/§E mention reader sweeps but this specific status→section map is not itemized).
  - Info-loss risk: LOW.

- **[MEDIUM] Same token, two tables, two meanings: `abgelehnt`/`reguliert` on faelle vs claims** · category: drift · effort: s · coveredByCMM49: partial
  - Evidence: CONFIRMED. `lifecycle.ts:135-137` reads `abgelehnt` from **claims.status** → `nachforderung`. `statusLabels.ts:49` labels `abgelehnt` as "Abgelehnt" in the faelle/Welle-7 set; `vs-abgelehnt` is a separate faelle enum value. claims CHECK admits both `reguliert` (legacy) and `reguliert_vollstaendig` (canonical) — verified verbatim in the constraint. **Verdict: [CONFIRMED].**
  - Recommendation: At §D3, drop the legacy `reguliert` from the CHECK (the finder's "verify zero writers" is the right gate — I did not exhaustively grep all writers, so this must be done before the DROP). Ensure `abgelehnt` has one meaning.
  - Info-loss risk: none (synonym consolidation).

- **[MEDIUM] Leads carry two disagreeing vocabularies: `leads.status` (lead_status enum, 6 live) vs `qualifizierungs_phase` (text, 9 live)** · category: redundancy · effort: m · coveredByCMM49: no
  - Evidence: CONFIRMED — both columns are real (I initially mis-tested the column name; the finder was right). `leads.status` is a `lead_status` ENUM (8 enum values: `kalt, rueckruf, quali-offen, flow-gesendet, umgewandelt, umgewandelt-sv, disqualifiziert, neu`), live = `flow-gesendet`:130, `disqualifiziert`:96, `umgewandelt`:72, `quali-offen`:30, `neu`:2, `umgewandelt-sv`:1 (331). `qualifizierungs_phase` is `text`, live = `flow-versendet`:130, `disqualifiziert`:69, `abgeschlossen`:64, `erstkontakt`:37, `neu`:14, `konvertiert`:8, `rueckruf`:5, `in-qualifizierung`:3, `gegner-daten`:1 (331). They disagree (disqualifiziert 96 vs 69; umgewandelt 72 vs konvertiert 8 vs abgeschlossen 64). `checkLeadAutoPhase` writes only `qualifizierungs_phase` (`autoPhase.ts:14-31`, verified — nothing keeps `status` aligned). `LEAD_PHASE_LABELS` (`statusLabels.ts:312-322`) has codes (`schadentyp-erfasst`, `konstellation-erfasst`, `gutachtertermin`, `sa-unterschrieben`) absent from both live sets. **Verdict: [CONFIRMED].** Note one nuance: `lead_status` is an ENUM (harder to drop than a text column — needs the enum retired too), `qualifizierungs_phase` is free text.
  - Recommendation: As finder — pick one. But note the asymmetry: making `lead_status` derived means dropping a Postgres ENUM type (more DDL than the finder implies). Audit dispatch-UI filters first.
  - Info-loss risk: LOW (counts already disagree → one is already unreliable).

- **[LOW] `phase_transitions` audit table has 1 row** · category: dead-code · effort: xs · coveredByCMM49: no
  - Evidence: CONFIRMED. `phase_transitions` = 1 row (verified; shared context said 0, finder corrected to 1 — finder is right). `state-machine.ts:235-246` inserts fire-and-forget on every transition; `timeline` also gets a `status-change` insert at `:225-231`. **Verdict: [CONFIRMED].**
  - Recommendation: Consolidate audit into `timeline` (which already captures the transition text); `phase_transitions`' extra fields (`trigger_type`/`actor_rolle`/`payload`) could move to a `timeline` JSONB. It needs re-keying `fall_id`→`claim_id` for the faelle drop anyway.
  - Info-loss risk: LOW.

- **[INFO] Proposed END-STATE: 2 stored status fields + everything derived** · category: simplification · effort: xl · coveredByCMM49: partial
  - Evidence: synthesis. **Verdict: [CONFIRMED as a direction], with a sharpened correction:** the finder's "2 stored fields = `claims.status` + `leads.qualifizierungs_phase`" undercounts. Sub-entity statuses (`auftraege.status`, `kanzlei_faelle.status`, `gutachten.status`) are NOT derivable — they ARE the inputs `v_claim_phase` reads (verified: the view's FROM is `claims LEFT JOIN kanzlei_faelle LEFT JOIN leads LEFT JOIN LATERAL auftraege`). So the honest end-state is "**2 claim/lead-level stored status fields + N sub-entity statuses (kept) + main/sub phase derived**." Framing them away as "everything else derived" risks implying the sub-entity statuses could vanish — they cannot; the derived view depends on them.
  - Recommendation: Adopt as direction, Aaron-gated. Keep sub-entity statuses explicit in the end-state diagram.
  - Info-loss risk: see HIGH findings above (granular 19-enum states must be re-homed first).

### Missed in this dimension

- **[HIGH] Shared-context claim "v_claim_phase STILL touches faelle" is REFUTED — and the finder didn't check it.** The live `v_claim_phase` definition (verified in full) is `FROM claims c LEFT JOIN kanzlei_faelle kf … LEFT JOIN leads l … LEFT JOIN LATERAL auftraege` — **no faelle reference at all**. `touches_faelle=true` in my probe is a false positive from the substring "faelle" inside "kanzlei_faelle". The view that genuinely still joins faelle is `v_claim_listing` (from_or_join_faelle=true, CONFIRMED). The finder accepted the model as "the documented target … is correct" without noticing the shared-context error about its faelle-dependency. Recommendation: correct the synthesis — `v_claim_phase` is already faelle-free (good, ready for §B); `v_claim_listing` is the phase-adjacent view still on faelle.

- **[MEDIUM] `v_claim_phase` has a latent vocabulary-drift bug the finder missed:** its sub_phase CASE emits `eg.status` raw (`WHEN eg.status IS NOT NULL AND eg.status <> 'abgeschlossen' THEN eg.status`). `auftraege.status` values are `termin/besichtigung/gutachten/abgeschlossen` — these happen to coincide with valid ClaimSubPhase tokens, so parity holds today. But if anyone adds an `auftraege.status` value (e.g. `nachbesichtigung`) without adding it to `ClaimSubPhase`, the SQL view and `lifecycle.ts` (which maps via an explicit `subMap` at `lifecycle.ts:201-206`) silently diverge — breaking the bit-parity gate. The parity is unguarded against sub-entity enum growth. Recommendation: add a CHECK or a parity test that asserts every `auftraege.status` value is a ClaimSubPhase.

- **[LOW] `FALL_STATUS_LABELS` (statusLabels.ts:7-51, ~40 codes) is a 7th vocabulary the finder mentioned only obliquely.** It merges enum values + Notion-matrix codes (`termin-reserviert`, `as-versendet`, `warten-auf-vs`) + Welle-7 Kanzlei codes (`in_bearbeitung`, `vs_kontakt`, `reguliert`, `kanzlei`) — many of which map to no live column at all (e.g. `termin-reserviert`, `as-versendet` are neither in the fall_status enum nor claims.status). It's still live (FallStatusBadge consumer) so not deletable wholesale, but it's the single largest accreted label bag and should be pruned to the union of {fall_status enum live values} ∪ {claims.status live values} when fall_status dies in §E.

### Open questions
- For Aaron: are the granular operational states (`vs-kuerzt`, `nachbesichtigung-laeuft`, `filmcheck`, `qc-pruefung`, `anschlussschreiben`) still operationally needed post-faelle-drop? The live kürzung-SLA (`state-machine.ts:322`) says yes for `vs-kuerzt`. If yes, §D3 must re-home them onto sub-entities (`kanzlei_faelle.status`/`gutachten.status`) — NOT let them die with faelle. This is the unresolved core of §A7/§D3.
- For Aaron: keep `claims.status` dual-purpose (dispatch flag + lifecycle terminal) with documentation, or split out a `claims.work_state`? 100% of current rows sit on the dispatch-flag half.
- Cross-session: `leads.status` is an ENUM (not text) — retiring it in favor of derived-from-`qualifizierungs_phase` requires dropping the `lead_status` type, more DDL than a text-column drop. Coordinate with whoever owns the lead/dispatch reader sweep.
- Cross-session: is the `v_claim_phase` bit-parity gate (`lifecycle.ts` vs SQL view) currently tested in CI, and does that test guard against `auftraege.status` enum growth? If not, the parity is fragile.