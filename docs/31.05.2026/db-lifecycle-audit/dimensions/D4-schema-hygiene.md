# Schema hygiene & consistency

Confirmed: `FALL_STATUS_TRANSITIONS` exists at state-machine.ts:17, consumed at :71. The 19-value hand-coded adjacency is real and has no home on `claims`. I have sufficient evidence across all findings. Writing the corrected report.

## Schema hygiene & consistency
**Summary:** Most of the finder's numbers hold up against live PROD, but several need scope/precision fixes and one causal claim is loose. The headline "83/181 unconstrained TEXT status columns" is **adjusted to 92/194** (the finder's own SQL returned 92 without_check, contradicting its prose "83"). The claims↔faelle drift is **confirmed at 74/74 status + 43/74 status_changed_at + 0 drift on shared aspect columns** — but the link direction the finder cited (`claims.fall_id`) **does not exist**; the real linkage is `faelle.claim_id → claims.id` (one-directional, every faelle row has a claim). The disjoint-vocabulary challenge (12 CHECK vals vs 19 enum vals, only `storniert` shared) is **fully confirmed and is the highest-value model-level finding**. The "5 base-table ENUMs" claim is **understated → 16 ENUM columns** (the finder omitted `betreuungspaket`, `bkat_unfallart`, `sv_paket`, plus 3 category enums). The `benachrichtigungen` dual-timestamp "likely bug" is **refuted**: both `created_at` and `erstellt_am` are populated on all 2598 rows. `parteien` is not a "live coexisting legacy table" — it has **0 rows** (a dead enum-carrying shell). The dokumente subsystem is real but is **18 files, not 39**, and the "still dual-written → 0 drift" causal attribution is imprecise (3 of the 6 zero-drift aspect columns are NOT at the cited lines).

### Findings

- **[HIGH] Unconstrained TEXT status/typ columns: ADJUSTED to 92 of 194 (not 83 of 181)**  ·  category: hygiene  ·  effort: l  ·  coveredByCMM49: no
  - Evidence: live re-run — 194 base-table TEXT `*status*`/`*typ*`/`*phase*`/`*rolle*` columns, **102 with a CHECK, 92 without**. The finder's prose said "83 of 181" but its own quoted SQL block (`without_check`) is the same shape that returns **92** here. Examples remain valid: `repairs.status`, `gutachten.status`, `auftraege.status`, `kanzlei_faelle.status`, `vehicles.status`, `nachrichten.sender_rolle` are bare `text`. (Note: my CHECK-detection is a crude column-name match against `constraint_column_usage`, so 92 is an upper bound — a multi-column CHECK can over-attribute; but the order of magnitude and the lifecycle-critical examples hold.)
  - Recommendation: unchanged — ratify "every status/typ/rolle col = `text` + `CHECK (col = ANY(ARRAY[...]))`" and backfill CHECKs on the lifecycle-feeders first (`auftraege.status`, `gutachten.status`, `kanzlei_faelle.status`, `repairs.status` — these feed `getClaimLifecycle()`). `ALTER ... ADD CONSTRAINT ... NOT VALID` then `VALIDATE` avoids a table rewrite.
  - Info-loss risk: none.
  - Verdict: **[ADJUSTED: 92/194, not 83/181 — the finder's denominator and numerator are both slightly off; severity HIGH stands]**

- **[HIGH] `claims.status` and `faelle.status` are disjoint vocabularies — two parallel state machines, not sync-rot**  ·  category: challenge  ·  effort: m  ·  coveredByCMM49: partial
  - Evidence: **CORRECTED LINKAGE** — there is no `claims.fall_id`; the join is `faelle.claim_id → claims.id`. Re-run on the correct column: **74/74 status differ, 43/74 status_changed_at differ, 0 faelle rows without a claim.** Value distributions: `claims.status` ∈ {`dispatch_done`=72, `in_bearbeitung`=3} (total 75 claims, 74 of which back a faelle); `faelle.status` ∈ {`sv-termin`=61, `ersterfassung`=12, `gutachten-eingegangen`=1}. The `claims_status_check` CHECK (verified live, exactly 12 values: `dispatch_done, in_bearbeitung, in_kommunikation_vs, reguliert, abgelehnt, an_externe_kanzlei_uebergeben, storniert, reguliert_vollstaendig, klage_rechtsstreit, verjaehrt, abgelehnt_final, termin_durchgefuehrt`) has **zero overlap** with the `fall_status` enum (verified live, 19 values incl. `besichtigung, begutachtung-laeuft, filmcheck, qc-pruefung, nachbesichtigung-laeuft, vs-abgelehnt`); only `storniert` is spelled the same. `FALL_STATUS_TRANSITIONS` (the 19-state adjacency) confirmed at `src/lib/faelle/state-machine.ts:17`, consumed at `:71` — it has no equivalent on `claims`.
  - Recommendation: unchanged and reinforced — this is the unresolved §A7/§D3 question. Before `faelle` drops, prove the 19 enum operational labels are reconstructable from the derived `subPhase` (18 codes in `v_claim_phase`), or `claims.status` (12 coarse vals) silently loses mid-flight resolution.
  - Info-loss risk: HIGH if mishandled — collapsing 19→12 loses `besichtigung`/`begutachtung-laeuft`/`filmcheck`/`qc-pruefung` granularity unless `subPhase` absorbs it.
  - Verdict: **[CONFIRMED — drift numbers exact (74/74, 43/74); disjoint-vocabulary thesis solid. One correction: the cited join column `claims.fall_id` does not exist (it is `faelle.claim_id`); the numbers nonetheless reproduce on the correct column. coveredByCMM49: partial is right — §A7/§D3 are open.]**

- **[HIGH] `claims` is a 173-col near-god-table; 7 aspect-`_status` cols overlap the live `pflichtdokumente`/`claim_parties` subsystems**  ·  category: simplification  ·  effort: l  ·  coveredByCMM49: partial
  - Evidence: **claims = 173 cols (confirmed live).** The 7 aspect-status cols are confirmed exactly: `vollmacht_status, vollmacht_pruefung_status, zb1_status, unfallmitteilung_status, polizeibericht_status, marketing_provision_status, kanzlei_provision_status`. `pflichtdokumente` exists (20 cols), `claim_parties` exists with **72 rows** (≈ the 74 claims). Document subsystem confirmed but **18 .ts files, not 39** (`katalog.ts`, `pflicht-evaluator.ts`, `anforderung.ts`, `zuordnung.ts`, `sichtbarkeit.ts` all present under `src/lib/dokumente/`). **Important nuance:** `polizeibericht_status` is **claims-only** (does not exist on faelle — confirmed via error), so it never dual-wrote; and the dual-write list in `claim-duplicate-columns.ts` literally contains only `polizeibericht_status` (L44), `unfallmitteilung_status` (L93), `zb1_status` (L97) (+ `technische_stellungnahme_status` L151) — NOT `vollmacht_status`/`vollmacht_pruefung_status`/the two provision statuses.
  - Recommendation: unchanged direction. (a) Fold the document-aspect statuses into `pflichtdokumente` rows + expose via `v_claim_doc_status`. (b) Move provision statuses to `makler_provisionen.status`/`provisionen_maik.status`. (c) Confirm `gegner_*`/`halter_*` snapshots land in `claim_parties` (CMM-63/65). Net-new beyond CMM-49 (which only de-`faelle`s columns, never de-`claims`).
  - Info-loss risk: low — document statuses recomputable from `pflichtdokumente`; provision statuses have authoritative homes.
  - Verdict: **[ADJUSTED: dokumente subsystem is 18 files not 39; pflichtdokumente=20 cols, claim_parties=72 rows (both confirmed). Core simplification thesis CONFIRMED. coveredByCMM49 corrected partial→no for the claims-side decomposition — CMM-49 explicitly does not touch claims columns.]**

- **[MEDIUM] claims↔faelle aspect-status sync: 0 drift CONFIRMED for the 6 shared columns, but the "because dual-written" mechanism is imprecise**  ·  category: drift  ·  effort: s  ·  coveredByCMM49: partial
  - Evidence: 6 of 7 aspect cols exist on both tables (all but `polizeibericht_status`). Live drift = **0 on all 6**: vollmacht_status=0, vollmacht_pruefung_status=0, zb1_status=0, unfallmitteilung_status=0, marketing_provision_status=0, kanzlei_provision_status=0. The finder is right that `status`/`status_changed_at` (74/43 drift) were severed while the aspect statuses stayed in sync. BUT only 3 of those 6 (`zb1_status, unfallmitteilung_status`, and faelle-absent `polizeibericht_status`) appear in `claim-duplicate-columns.ts` at the cited lines — `vollmacht_status`, `vollmacht_pruefung_status`, and both provision statuses show 0 drift yet are NOT at L44/93/97. So either they're synced via a different mechanism (trigger) or simply haven't been written since the split. The finder's blanket "they're in claim-duplicate-columns.ts:44,93,97 (dual-written)" over-attributes.
  - Recommendation: before relying on "aspect statuses are safe to drop with faelle", confirm the actual sync source for `vollmacht*`/provision statuses (trigger vs dual-write vs frozen). If they're frozen-but-equal (not actively synced), that's fine for the drop; if a trigger syncs them, it joins the §F "drop sync triggers" surface.
  - Info-loss risk: none.
  - Verdict: **[ADJUSTED: 0-drift CONFIRMED for 6 shared cols; the dual-write attribution is OVERSTATED for 3 of them. This was buried inside the finder's HIGH challenge finding — promoting it as its own MEDIUM because the mechanism matters for the drop.]**

- **[MEDIUM] Timestamp naming split CONFIRMED — 10 mixed tables, 31 German, but `benachrichtigungen` is NOT a bug**  ·  category: hygiene  ·  effort: m  ·  coveredByCMM49: no
  - Evidence: live counts — `created_at`×92 ✓, `updated_at`×**53** (finder's headline said 53 in one place, 59 in shared-context — 53 is correct), `erstellt_am`×27 ✓, `aktualisiert_am`×4 ✓, `geaendert_am`×1 ✓, `erstellt_von`×5 ✓, `geaendert_von`×1, `created_by`×0 ✓. Mixed-convention tables = **10, list matches the finder exactly** (admin_termine, anruf_log, auftraege, benachrichtigungen, communities, dokument_upload_anfragen, embed_abrechnung_positionen, embed_sites, kanzlei_faelle, timeline). 31 tables carry German timestamps ✓. **`benachrichtigungen` correction:** both `created_at` AND `erstellt_am` are populated on **all 2598 rows** — not a "two timestamps for one event" bug to collapse; it's a redundant-but-consistent mirror (likely a generated/synced column). Removing one is still a valid simplification, but the "verify which is populated / likely a bug" framing is wrong — both are.
  - Recommendation: unchanged for the freeze + Boy-Scout de-mix. For `benachrichtigungen`, treat as a redundant mirror to consolidate (keep `created_at`, drop `erstellt_am` after confirming no reader hard-codes `erstellt_am`), not as a data-integrity bug.
  - Info-loss risk: none — both columns hold the same value.
  - Verdict: **[ADJUSTED: counts CONFIRMED (updated_at=53); `benachrichtigungen` dual-timestamp REFUTED as a bug — both fully populated on 2598/2598 rows.]**

- **[MEDIUM] ENUM footprint UNDERSTATED — 16 ENUM columns, not 5**  ·  category: simplification  ·  effort: s  ·  coveredByCMM49: partial
  - Evidence: live — **16 base-table columns are Postgres-ENUM-typed** across ~9 tables: `faelle.status`(fall_status), `leads.status`(lead_status), `tasks.status`(task_status), `parteien.rolle`(partei_rolle), `parteien.vertrag_typ`(vertrag_typ), `profiles.rolle`(user_role) — the 6 *state/role* enums the finder counted (it said 5, actually 6 distinct cols) — PLUS `claims.betreuungspaket`(betreuungspaket), `faelle.betreuungspaket`, `claims.bkat_unfallart`(bkat_unfallart), `faelle.bkat_unfallart`, `leads.bkat_unfallart`, `bkat_tatbestaende.schuldindiz`(bkat_schuldindiz), `bkat_tatbestaende.unfallart`, `profiles.sv_paket`(sv_paket_typ), `dokument_katalog.kategorie`(dokument_kategorie), `schadenspositionen.kategorie`(schadens_kategorie). So ~12 distinct enum types, not "5 outliers". The "CHECK strings dominate" thesis still holds directionally, but the ENUM cleanup surface is 3x larger than stated.
  - Recommendation: unchanged policy ratification ("no new ENUMs"). Net-new correction: the ENUM→TEXT+CHECK conversion list is bigger — `betreuungspaket`, `bkat_unfallart`, `sv_paket_typ`, `dokument_kategorie`, `schadens_kategorie`, `bkat_schuldindiz` are additional candidates beyond the 6 state enums. `fall_status`+`parteien.*` die with the table drops; `user_role` stays the RLS-sensitive priority.
  - Info-loss risk: none — ENUM→TEXT+CHECK preserves values; only `enumsortorder`-based `ORDER BY` needs a CASE map (`fall_status` has fractional sortorders 1.5/1.75/8.625 — a phase-ordering signal that must be ported to a sort-map if any query relies on enum ordering).
  - Verdict: **[ADJUSTED: 16 ENUM columns / ~12 enum types, not 5. The finder silently scoped to status/typ/rolle and missed the package/category enums. Thesis intact, footprint corrected.]**

- **[LOW] FK gaps CONFIRMED — 97/405 `*_id` cols lack a FK; ON DELETE distribution exact**  ·  category: hygiene  ·  effort: s  ·  coveredByCMM49: partial
  - Evidence: live — **405 `*_id` columns, 97 without a FK (24%) — confirmed exactly.** ON DELETE distribution across 326 FKs: **CASCADE 128, SET NULL 106, NO ACTION 83, RESTRICT 9** (128+106+83+9=326) — all confirmed exactly. The external-system-ID exemption (Stripe/GCal/Salesforce/`place_id`) is sound and the genuinely-internal gap list (`claims.makler_id`, `claims.abrechnung_id`, `faelle.kanzlei_id`, etc.) is reasonable (not individually re-verified, but the count and direction are solid).
  - Recommendation: unchanged — add FKs (`NOT VALID` then `VALIDATE`) on the ~10 internal cols; audit the 83 NO-ACTION FKs before the faelle CASCADE drop and for `dsgvo_anonymize_user_data`.
  - Info-loss risk: none.
  - Verdict: **[CONFIRMED — 97/405 and the 128/106/83/9 ON-DELETE split are exact.]**

- **[INFO] `parteien` is NOT a live legacy table — it has 0 rows (dead enum-carrying shell)**  ·  category: dead-code  ·  effort: s  ·  coveredByCMM49: partial
  - Evidence: live — `parteien` exists but **0 rows**; `claim_parties` has **72 rows**. So `parteien` is the last home of the `partei_rolle`/`vertrag_typ` enums AND it is already empty. The finder framed it as "two party tables coexist, one with the old ENUM convention" — accurate on schema, but it's an empty shell, not a live coexisting table. This actually strengthens the drop case (no data migration needed) and reclassifies it from `redundancy` to `dead-code`.
  - Recommendation: stronger than the finder's — `parteien` can be dropped outright (0 rows) pending a reader-sweep; it does not need a `claim_parties`-superset data migration. Its drop kills the `partei_rolle`+`vertrag_typ` enums for free. Confirm it's on the CMM-49 §C / drop list; if any code still SELECTs `parteien`, those reads now return empty (silent behavior change worth grepping).
  - Info-loss risk: none — 0 rows.
  - Verdict: **[ADJUSTED: parteien=0 rows (dead shell), not a live coexisting table; reclassify redundancy→dead-code. Drop case strengthened.]**

### Missed in this dimension
- **[MEDIUM] `polizeibericht_status` is a claims-only orphan with no faelle twin — a one-off that breaks the "aspect columns are faelle-mirrored" mental model.**  ·  category: hygiene  ·  effort: xs  ·  coveredByCMM49: no
  - Evidence: of the 7 claims aspect-status cols, 6 exist on faelle; `polizeibericht_status` does **not** (live error confirmed). Yet `claim-duplicate-columns.ts:44` lists `polizeibericht_status` in the duplicate-columns array — meaning the dual-write list references a column that exists on only one side. That's either a latent bug (a write that targets a non-existent faelle column would error) or a defensively-guarded no-op. Worth a grep of how that array is consumed.
  - Recommendation: verify `claim-duplicate-columns.ts` consumers guard against faelle-absent columns; if the array is used to build an UPDATE on faelle, `polizeibericht_status` should be removed from it.
  - Info-loss risk: none.

- **[LOW] `fall_status` enum carries fractional `enumsortorder` values (1.5, 1.75, 3.5, 3.75, 8.625, 8.75, 8.875) — an encoded phase-ordering that is NOT captured anywhere in the claims model.**  ·  category: challenge  ·  effort: s  ·  coveredByCMM49: no
  - Evidence: live `pg_enum` — the 19 labels were inserted with fractional sort orders to wedge new operational states between existing ones (e.g. `besichtigung`=3.5 between `sv-termin`=3 and `gutachten-eingegangen`=4). This is a second, implicit phase-progression signal living in the enum metadata. When `fall_status` dies, this ordering is lost unless `v_claim_phase.subPhase` encodes the same monotonic progression. Reinforces the §A7/§D3 risk from a different angle: it's not just the labels, it's their *order* that the legacy model encodes.
  - Recommendation: when validating the 19→18 subPhase mapping (§A7), also confirm the subPhase ordering preserves the fractional-sortorder progression, or any "show cases past phase X" query loses meaning.
  - Info-loss risk: medium if a query relies on `ORDER BY status` (enum sort) — grep for `ORDER BY` on faelle.status before drop.

### Open questions
- §A7/§D3 (the core unresolved lifecycle question): does `claims.status` (12 coarse CHECK values) need to preserve the 19-value `fall_status` operational granularity AND its fractional sort-order progression, or is that fully delegated to derived `subPhase` (18 codes)? Do all 19 enum labels map 1:1 onto the 18 subPhase codes, or do `besichtigung`/`begutachtung-laeuft`/`filmcheck`/`qc-pruefung` become indistinguishable?
- Where does `FALL_STATUS_TRANSITIONS` (state-machine.ts:17, the 19-state adjacency) live once `faelle.status` is gone — dropped entirely in favor of derived phase, or ported onto `claims.status` (12 values)?
- What actually keeps `vollmacht_status`/`vollmacht_pruefung_status`/the two `*_provision_status` columns at 0 drift, given they're NOT in `claim-duplicate-columns.ts` at the cited lines — a trigger, or simply no writes since the split? (Determines whether they're part of the §F sync-trigger drop surface.)
- `claim-duplicate-columns.ts:44` lists `polizeibericht_status`, which exists only on `claims` (not `faelle`). Is that a latent dual-write bug or a guarded no-op?
- `benachrichtigungen` has `created_at` and `erstellt_am` both populated on all 2598 rows — confirm the mirror source (generated column? trigger?) before collapsing to one.
- Policy ratification: "CHECK strings, never new Postgres ENUMs" — and the ENUM→CHECK conversion list is **16 columns / ~12 types**, not 5. Should the package/category enums (`betreuungspaket`, `bkat_unfallart`, `sv_paket_typ`, `dokument_kategorie`, `schadens_kategorie`) be in scope, or only the state/role enums? Prioritize `user_role` (RLS-escalation hotspot)?
- `parteien` is empty (0 rows). Confirm it's explicitly on the drop list (it carries the last `partei_rolle`/`vertrag_typ` enum consumers); grep for any remaining `parteien` SELECTs that would silently start returning empty.