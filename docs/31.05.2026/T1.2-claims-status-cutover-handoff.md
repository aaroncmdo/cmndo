# T1.2 — Handoff: `fall_status` → `claims.status` Cutover (CMM-49 Lifecycle-Achse)

**Datum:** 31.05.2026 · **Session:** 0243cdab · **Track:** CMM-49 Track 1 (Lifecycle-Freeze), Sub-Phase T1.2
**Status:** b′ + c gebaut & gemergt, d zu 1/N, b‴ vorbereitet (0 Rows), b″ offen. **Convert entsperrt, staging sauber.**
**Single Source of Truth für T1.2** — dieser Plan ist verbindlich, keine Abweichung ohne Update hier.

---

## 0 · Kontext in drei Sätzen

CMM-49 entfernt `faelle` zugunsten von `claims` als SSoT. **D2 (work_state-Split) ist fertig**: `claims.status` = Lifecycle/Terminal-Achse, `claims.work_state` = Dispatch/Processing-Achse. **T1.2** ist der Cutover der Lifecycle-Achse: die Engine (`state-machine.ts::transitionFallStatus`) soll aufhören, `faelle.status` (19-Enum) zu schreiben, und stattdessen `claims.status` (12-CHECK) + Sub-Entity-Heimaten schreiben; die Reader lesen die Phase aus `v_claim_phase` (faelle-frei).

---

## 1 · Daten-Realität (live, 31.05., GETEILTE prod+staging-DB `paizkjajbuxxksdoycev`)

- **claims:** 76 Rows, **status = NULL bei allen 76** (T1.1c nullte die 75 Dispatch-Rows; keine Terminals gesetzt).
- **faelle:** 75 Rows, **alle aktiv**: `sv-termin` 62 / `ersterfassung` 12 / `gutachten-eingegangen` 1. **Keine** Terminal-/VS-Fälle.
- **kanzlei_faelle:** 12 Rows, alle `status='versicherungskontakt'`.
- **Konsequenz (de-risk):** Es gibt aktuell KEINE geschlossenen/VS-Fälle. → b‴-Backfill = **0 Rows**. → Kein „stale-closed-Display"-Problem. → `v_claim_phase` leitet alle 76 claims korrekt als aktiv aus Sub-Entities ab (status=NULL-Pfad). Das Mapping/Backfill ist trotzdem für die Zukunft korrekt zu bauen.

---

## 2 · Der Plan: Dual-Write-Bridge (b′ → c → b‴ → d → b″)

Statt „faelle.status-Write hart stoppen" (bräche die Rest-Reader) — additiv in Schritten, riskanter Stopp zuletzt:

| Schritt | Was | Status | Gate |
|---|---|---|---|
| **b′** | Engine schreibt `claims.status` **additiv** via Mapping; `faelle.status`-Write bleibt | ✅ **#2151 MERGED** | — |
| **c** | Mapping ↔ `v_claim_phase` konsistent (kein Orphan); zahlung-Fix | ✅ **#2153 MERGED** + L2-Smoke | — |
| **b‴** | claims.status-**Backfill** für Alt-Terminal-Fälle (Safety-Net) | 🟡 **vorbereitet, 0 Rows** | careful Shared-DB-Write → fokussierter Run |
| **d** | Reader-Repoint: 1 Cron ✅ + 4 Display + ~6 KPI-Filter | 🟡 **1/N (#2156 Cron MERGED)** | Display/KPI: b′-live (de-gated da all-active) |
| **b″** | `faelle.status`-Write final aus der Engine raus | 🔴 offen | NACH d komplett + kein Reader liest fall_status |

**Warum diese Reihenfolge:** jeder Schritt einzeln mergebar+smokebar; `faelle.status` bleibt bis b″ als Schatten → kein Reader bricht zwischendurch.

---

## 3 · Das Mapping (`faelle.status` → `claims.status`) — c-validiert

Code: `src/lib/faelle/fall-status-claim-mapping.ts` (`mapFallStatusToClaimStatus`, 26 Unit-Tests). In der Engine verdrahtet (b′).

| faelle.status | → claims.status | v_claim_phase main/sub | Begründung |
|---|---|---|---|
| storniert | `storniert` | abschluss / storniert | Terminal |
| abgeschlossen | `reguliert_vollstaendig` | abschluss / erfolgreich_reguliert | Happy-Path-Terminal; **Guard:** überschreibt bestehenden härteren Terminal nicht |
| vs-abgelehnt | `abgelehnt` | regulierung / nachforderung | nicht `_final` — kann → klage eskalieren |
| regulierung, regulierung-laeuft | `in_kommunikation_vs` | regulierung / versicherungskontakt | VS-Verhandlung |
| **zahlung-eingegangen** | **`null`** (KEIN Write) | (claim_payments trägt's) | `reguliert` wäre v_claim_phase-Orphan (c-Befund, #2153) |
| ersterfassung … gutachten-eingegangen (aktiv) | `null` | derived (Sub-Entities) | via gutachter_termine/auftraege |
| filmcheck, qc-pruefung | `null` | derived | gutachten/auftraege QC |
| kanzlei-uebergeben, anschlussschreiben, nachbesichtigung-laeuft | `null` | derived (kanzlei_faelle / auftraege) | Sub-Entity trägt Granularität |

**Nicht im fall_status-Enum** (19 Werte): `klage`, `vs-kuerzt` — kommen also nie als faelle.status (werden anders/direkt geschrieben). Das Mapping kennt `klage`→`klage_rechtsstreit` trotzdem (für ggf. direkte Caller), schadet nicht.

**Pflicht-Invariante (c):** jeder von b′ gesetzte `claims.status`-Wert MUSS ein `v_claim_phase`-CASE-Zweig sein (sonst falsche Phasen-Ableitung). v_claim_phase kennt status-seitig: 7 Terminals + `in_kommunikation_vs` + `abgelehnt`. **Nur diese setzen.**

---

## 4 · Session-Findings (entdeckte Ungereimtheiten — alle gefixt/dokumentiert)

1. **Convert-Root-Cause = `claims.status` DEFAULT `'offen'`** (1601e3a6-Fund): T1.1b entfernte den expliziten status-Write in convert-lead-to-claim → unset fiel auf DEFAULT `'offen'` → nicht im CHECK → JEDE Claim-Erzeugung brach. Fix: `DROP DEFAULT` (**Migration 20260531203552**, appliziert+getrackt). **File-Ownership offen** (Hook-Kollision blockte mich; 1601e3a6 PRt es standalone, oder Aaron `force-edit-collision` → ich ziehe es in track1). Verifiziert: default `<none>`, version getrackt, 0 'offen'-claims.
2. **`vs-kuerzt` ist kein fall_status-Enum-Wert** → der alte vs-korrespondenz-Cron-Filter trug einen toten Literal (gefixt mit #2156).
3. **claims_status_check-Hotfix** (#2141): T1.1c-Tightening war verfrüht (Shared-DB, nicht alle Writer migriert) → re-permissiv.
4. **`v_claim_full.phase`** (#2143): get-claim-for-role whitelistete eine gedroppte Spalte → kunde/sv/kanzlei-Reads gebrochen.
5. **`faelle_sv_view`-Drift** (#2146): CMM-50-3a exponierte 2 SV-Spalten nicht mehr → SV-Fall-Detail gebrochen.
6. **RLS-Rekursion `sv_buero_memberships`** (#2148): Policy referenzierte eigene Tabelle → SECDEF-Helper `is_buero_admin`.

---

## 5 · PR-Ledger (Session 0243cdab, 31.05.)

| PR | Inhalt | Status |
|---|---|---|
| #2141 | claims_status_check Hotfix (re-permissiv) | MERGED |
| #2143 | v_claim_full.phase Whitelist-Fix | MERGED |
| #2146 | faelle_sv_view 2 SV-Spalten | MERGED |
| #2148 | sv_buero_memberships RLS-Rekursion | MERGED |
| #2151 | **b′** — Engine claims.status Dual-Write | MERGED |
| #2153 | **c** — zahlung-Mapping-Fix (v_claim_phase-Konsistenz) | MERGED |
| #2156 | **d (1/N)** — vs-korrespondenz-Cron auf kanzlei_faelle | MERGED |
| — | (fremd) DROP DEFAULT `claims.status` (Mig 20260531203552, 1601e3a6) | appliziert, File-PR offen |

---

## 6 · Remaining-Map (Reihenfolge + Gate)

1. **b‴ — claims.status-Backfill anwenden** (Safety-Net, 0 Rows jetzt). SQL siehe §8. Via `apply_migration`, dann File committen (Regel 2). Careful Shared-DB-Write → fokussierter Run.
2. **d-rest — Display + KPI-Reader** (de-gated da all-active; b′-live verbessert künftige Genauigkeit):
   - 4 Display-Badges (`mitarbeiter/page:200`, `mitarbeiter/faelle:49`, `admin/statistiken:156`, `mitarbeiter/isochrone:27`) → `sub_phase` + **bestehenden** Helper `substateLabelForRolle`/`SUBPHASE_LABEL` (`lib/fall/subphase-visibility.ts`, NICHT neu bauen).
   - ~6 „abgeschlossen"-KPI-Filter (`admin/team[/id]`, `admin/team/leaderboard` ×3, `mitarbeiter/performance` ×2): `fall_status='abgeschlossen'` → **`sub_phase='erfolgreich_reguliert'`** (Aaron-entschieden: nur erfolgreich reguliert, schließt storniert + klage-resolved aus).
   - Sonderfälle: `fall_status_at_generation` (faq-bot/ai-actions Snapshot, KEIN Reader — bleibt) · `makler/queries.ts:1007` liest faelle.status direkt (eigener Repoint).
   - Je Reader: Build + Portal-Smoke + Screenshot.
3. **b′ L3-Smoke** — echter staging-Übergang durch die deployte Engine → `claims.status` prüfen. Convert ist entsperrt → kommt beim Testen organisch; ich verifiziere observational via `phase_transitions` + claims.status. (L2-Smoke = Output-Kontrakt — schon grün.)
4. **b″ — faelle.status-Write raus** — erst wenn d komplett + kein Reader mehr fall_status liest. Dann stirbt `fall_status` (19-Enum) + `FALL_STATUS_TRANSITIONS` mit dem faelle-Drop (CMM-49 §G).

---

## 7 · Gates / Koordination

- **state-machine.ts-Lock:** Lane frei (alle 939-Branches identisch mit staging auf der Datei; letzter Touch #1760). Vor jedem Engine-Edit Lock re-prüfen + Collision-Hook respektieren.
- **DROP-DEFAULT-File (Mig 20260531203552):** Ownership-Frage offen — entweder 1601e3a6 PRt standalone, oder Aaron `force-edit-collision` → 0243cdab zieht es in die track1-Strecke.
- **KEIN neuer claims.status-DEFAULT** geplant (status = NULL-bis-Terminal). Falls doch → mit 1601e3a6 koordinieren (Doppel-Migration vermeiden).
- **Andere claims.status-Direktschreiber** (embed-B `closeNurGutachter`→`termin_durchgefuehrt`, dispatch) bleiben kanonisch; die Engine wird dazu konsistent, ersetzt sie nicht. Bei b″ deren Werte im CHECK sicherstellen.

---

## 8 · b‴ Backfill — fertige Migration (dry-run-verifiziert, 0 Rows jetzt)

Migration-Name: `track1_claims_status_backfill_terminal_vs_from_faelle`. Nur wo `claims.status IS NULL` (kein Clobber bestehender Werte / Direktschreiber).

```sql
-- b‴ (T1.2): Einmal-Backfill bestehender Terminal-/VS-faelle -> claims.status, damit
-- claims.status auch fuer VOR b' geschlossene Faelle die Realitaet traegt (b' deckt nur
-- kuenftige Uebergaenge). Nur wo claims.status IS NULL (kein Clobber). Mapping == b'
-- (fall-status-claim-mapping.ts). Stand 31.05.: 0 Rows (alle faelle aktiv) -> Safety-Net.
UPDATE public.claims c
SET status = CASE f.status
    WHEN 'storniert'          THEN 'storniert'
    WHEN 'abgeschlossen'      THEN 'reguliert_vollstaendig'
    WHEN 'vs-abgelehnt'       THEN 'abgelehnt'
    WHEN 'regulierung'        THEN 'in_kommunikation_vs'
    WHEN 'regulierung-laeuft' THEN 'in_kommunikation_vs'
  END
FROM public.faelle f
WHERE f.claim_id = c.id
  AND c.status IS NULL
  AND f.status IN ('storniert','abgeschlossen','vs-abgelehnt','regulierung','regulierung-laeuft');
```

`zahlung-eingegangen` bewusst NICHT (→ claim_payments, kein claims.status; analog b′). `klage`/`vs-kuerzt` nicht im Enum → entfallen.

---

## 9 · Lektionen (in Memory, hier konsolidiert)

- **Shared prod+staging-DB:** ein CHECK/DEFAULT nie ändern, ohne die Interaktion mit dem ÄLTESTEN laufenden Writer zu prüfen. T1.1c (CHECK zu früh verschärft) + der `'offen'`-DEFAULT (durch entfernten expliziten Write freigelegt) sind beide Varianten davon. Commit-auf-main ≠ deployter Prozess.
- **c-Validierung vor d:** jeder neue claims.status-Wert gegen `v_claim_phase` prüfen (sonst Orphan → falsche Phase). Fing den `reguliert`-Bug.
- **Empirie vor Aussage:** breites get_logs + live execute_sql; Annahmen (z.B. „Deploy-Lag" als Convert-Ursache) gegen Daten prüfen.
- **Engine-Smoke:** L2 (Output-Kontrakt, read-only, selbst machbar) + L3 (echter Übergang). L2 deckt das meiste; L3 organisch.
