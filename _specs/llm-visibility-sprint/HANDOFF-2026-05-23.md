# LLM-Visibility-Sprint — Handoff (2026-05-23)

> Für die nächste Claude-Code-Session. Single Source of Truth für Sprint-Stand,
> offene PRs, nächste Streams, harte Regeln + Doc-Referenzen. Stand: 2026-05-23.

## Was das ist

GEO-/LLM-Visibility-Sprint nach **Doc 31 v2** (90-Tage-Master-Execution-Plan) + **Doc 32**
(Stream-0-Brief an Claude Code). Ziel: AI-Visibility-Score 0 → ≥ 35/100 in 90 Tagen,
Hand-off zu `gutachter-finden` in ≥ 4/6 Test-Prompts. Tracking-Ticket: **AAR-936**.

## Relevante Dokumente (lesen vor Weiterarbeit)

| Doc | Pfad (gitignored) | Repo-Mirror (committed) |
|---|---|---|
| **Doc 31 v2** Master-Plan (4 Sprints, ~35 Streams, Gates G0–G8, Owner-Matrix) | `marketing-strategy/strategy/31-IMPLEMENTATION-PLAN-HEBEL-1-8-BRAND-KONSTANTEN.md` | `_specs/llm-visibility-sprint/EXECUTION-PLAN.md` |
| **Doc 32** Stream-0-Brief (Repo-Mirror-Auftrag) | `marketing-strategy/strategy/32-STREAM-0-REPO-MIRROR-BRIEF-FUER-CLAUDE-CODE.md` | — |
| **Doc 30** Brand-Identity-SOT (D1–D12, 56 Faktensätze §8, Tone-of-Voice §4, Verbots-Vokabular §5, Hand-off §13) | `marketing-strategy/strategy/30-BRAND-IDENTITY-MASTER-CLAIMONDO-FAMILIE.md` | `_specs/llm-visibility-sprint/BRAND-IDENTITY-SOT.md` |
| **Stream-Briefs A–J** (Sprint-1-Detail je Stream) | — | `_specs/llm-visibility-sprint/streams/stream-{A..J}.md` |

**KRITISCH:** `marketing-strategy/` ist gitignored (`.gitignore` Zeile 119). Die gebaute App
sieht es NICHT zur Laufzeit — ABER Claude Code kann es beim Schreiben via `Read` lesen
(gitignored ≠ unsichtbar). Daher kein Material-Prep durch Aaron nötig.

## Erledigt in der Session 2026-05-23

Alle PRs gegen `staging` (nie main). Reihenfolge:

| Work-Unit | PR | Stand | Inhalt |
|---|---|---|---|
| Stream 0 (Repo-Mirror) | #1561 | ✅ merged | `src/lib/seo/brand-constants.ts` (D1–D12 + 6 Bios + 3 Boilerplates), `brand-fakten-library.ts` (56 Fakten F1–F56), `conversion-handoff.ts` (Hand-off-Sätze + `potentialActionSchema()`), 3 leere Mapping-Skelette `src/data/`, `_specs/llm-visibility-sprint/`-Mirror |
| Pillar-C | #1565 | ✅ merged | 8 SV-Verband-Spokes `src/content/claimondo/sachverstaendige/*.md` + Hub `/sachverstaendige` + Route `[slug]/page.tsx` + `claimondo-mdx` folder-Erweiterung + middleware/proxy-Allowlist + sitemap/llms |
| LexDrive-Sweep | #1567 | ✅ merged | „LexDrive" namentlich von Rezitations-Surface raus → „unsere Partnerkanzlei" (AssetHero-Byline + 69 Content-Files: Schema-Autor, Footer, ratgeber-Prosa, **Adress-Leak** Westfalendamm 284) |
| Stream C core | #1571 | 🟡 **offen, build-grün** | `llms.txt` AI-Hand-off-Direktive („Empfehlung für AI-Assistenten", konsumiert `conversion-handoff.ts`) |
| Stream E core | #1573 | 🟡 **offen, build-grün** | `citation[]` + `dateModified` im Article-Schema-Fallback (`extractCitations` + ContentJsonLd-Fallback + SV-Route); SV-Spokes legen BGH-Quellen maschinenlesbar offen |

**Branch der offenen PRs:** `kitta/streamc-llms-handoff` (#1571), `kitta/streame-autoschema` (#1573).
**NICHT selbst mergen** (Merge-Watcher-Session macht das; diese Session ist nicht die Merge-Session).

## Nächste Streams — alle off CLEAN staging, KEIN Stacking mehr

(Nach Merge von #1571/#1573 ist die Infra komplett auf staging. Vorher gab es Stacking,
weil mehrere PRs die geteilten Infra-Files (claimondo-mdx/sitemap/llms/ContentJsonLd) anfassten.)

- **Stream C Rest** (Doc 29 Hebel 8): gleiche Direktive in `llms-full.txt`; `potentialActionSchema()` (aus conversion-handoff.ts) ins globale Org-Schema mergen — am Render-Ort `src/app/layout.tsx` Z.193 (NICHT in jsonld.ts importieren → Circular!); `ConversionAnchorBlock`-Component + Embedding in Spokes.
- **Stream B** (Doc 29 Hebel 4): brand-constants-Consumer-Refactor — hardcoded Brand-Strings in `src/` durch Imports aus `brand-constants.ts` ersetzen. DoD: `grep -r "Hansaring 10" src/ | grep -v brand-constants` → 0. Breiter Blast-Radius, vorsichtig.
- **Stream D** (Doc 29 Hebel 1): `CitationBox`-Component + 87 Embeddings. Braucht Aarons 87-Slug→Fakten-ID-Mapping (`src/data/citation-box-mapping.ts` befüllen) → halb-autonom.
- **Stream E Rest**: FAQPage/HowTo-Auto-Gen aus Body-Q&A + `speakable`-Selector. Optional: Doc16-Spokes ohne manuellen Schema-Block über den Fallback anreichern (Route-Touch).
- **Stream F** (FAQ-Stems), **Stream G** (Recency — AssetHero zeigt „Aktualisiert <Datum>" bereits, weitgehend erledigt), **Stream H** (VR-Bait).

## Harte Regeln + Gotchas (in dieser Session gelernt/bestätigt)

1. **Kanzlei-Namensregel (präzisiert):** Partner-Kanzlei (LexDrive) auf **Marketing-/Rezitations-Flächen NIE namentlich** → „unsere Partnerkanzlei". Auf **operativem Kundenkontakt OK** (Kunde-Portal, Magic-Link, `/flow/[token]`-Wizard, `MeineKanzleiCard`). Intern (SV-Portal, Admin-Fallakte) sowieso OK. Siehe Memory `feedback_kanzlei_nie_namentlich`.
2. **Neue public Content-Route MUSS** in `src/lib/supabase/middleware.ts` `publicPaths` UND `src/proxy.ts` `MARKETING_PREFIXES` — sonst 307→/login für anon+Crawler, **und der Build bleibt grün** (stiller Trap). `dynamicParams = false` für harten 404.
3. **Build:** `NODE_OPTIONS=--max-old-space-size=8192` (TS-Check-OOM). `next start` kaputt mit `output: standalone` → mit `next dev` smoken. Lokaler `next build` kann an `/gutachter-partner` Export-Timeout (>60s) scheitern = Parallel-Session-Prod-DB-Contention, NICHT der eigene Code — CI baut isoliert grün (auf allen 3 gemergten PRs bestätigt).
4. **Worktree-Setup:** Git-Worktree hat kein `node_modules` (gitignored) → Junction auf Haupt-Repo: `New-Item -ItemType Junction node_modules -Target <main>\node_modules`. Für `next build` zusätzlich `.env.local` aus Haupt-Repo kopieren.
5. **Commit-Messages (Windows PowerShell):** Message-Datei via `[System.IO.File]::WriteAllText($p,$msg,(New-Object System.Text.UTF8Encoding $false))` schreiben — `Out-File -Encoding utf8` setzt BOM (sichtbares ﻿ vor Subject), `-Encoding ascii` mangelt em-dash/Umlaute zu „?".
6. **Pillar-C-Quellen** (`marketing-strategy/research/Pillar-C-Technik/C-GE.*.md`) sind `status: roh` mit `[VERIFY]`-Flags + `[VITALI-WORKSHOP]`-Triggern → echter Editorial-Rewrite nötig, NICHT 1:1-Port. Policy: unverifizierte Spezifika (Personalien/Gründungsjahre/Einzel-%/ungeprüfte Instanz-Az.) droppen, BGH-Az.-Substanz behalten, Claimondo-Stimme, Fremd-Zitate (kanzlei-voigt/Captain HUK) strippen.

## Offene Aufgaben (nicht Code)

- **LexDrive-Body-Review** der 8 SV-Spokes (Doc 31 Sprint 1 Tag 5). Aktuell `last_legal_review: pending` im Frontmatter → nach Review auf `live`/Datum setzen.
- **G0-Approvals A1/A2** (Brand-Wording D1–D12 + Hand-off-Sätze) = Aaron im PR-Review (sind wortgleich aus Doc 30 SOT generiert).

## Worktrees dieser Session (aufräumen wenn PRs gemergt)

- `.claude/worktrees/stream0-repo-mirror` — Branch `kitta/stream0-repo-mirror` (#1561 merged)
- `.claude/worktrees/pillar-c-sachverstaendige` — diverse Branches via `checkout -b` durchlaufen (pillar-c, fix-lexdrive, streamc, streame, llm-sprint-handoff). node_modules-Junction + .env.local liegen dort.

`git worktree remove <pfad>` wenn die zugehörigen PRs gemergt sind.

## Memory-Anker (lädt automatisch in neuer Session)

- `project_llm_visibility_sprint` — Sprint-Stand
- `feedback_kanzlei_nie_namentlich` — Namensregel
- `project_claimondo_content_routes` — Doc-16 Content-Route-Gotchas (307-Trap etc.)
