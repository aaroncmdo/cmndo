// Tailwind v4 PostCSS-Plugin (identisch zu claimondo-v2 und claimondo-marketing).
// Tokens in app/globals.css @theme.
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}

export default config
